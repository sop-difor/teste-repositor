# AUDITORIA_INDEX.md — Auditoria de `index.html` (GECOPE)

## PROGRESSO
- [x] Passo 0.1 — Mapeamento de arquivos referenciados pelo `index.html`
- [x] Passo 0.2 — Árvore de dependências
- [x] Passo 0.3 — Verificação de outros pontos de entrada (`cronograma.html`)
- [x] Bloco 1 — Segurança: segredos/credenciais expostos (varredura completa em `config.js`, `admin.js`, `whatsapp.js` e histórico git)
- [x] Bloco 2 — Segurança: XSS (innerHTML), eval/Function, target=_blank, sessão/permissão (amostragem em `main.js`, `admin.js`, `index.html`)
- [x] Bloco 3/4/5 (parcial) — Bugs, código morto e desempenho: varredura por amostragem/grep (IDs duplicados, console.log, lazy-loading, promises sem catch)
- [x] Bloco profundo — `admin.js` + `whatsapp.js` + `relatorio.js` + `dashboard.js` (revisão linha a linha completa via agente dedicado)
- [x] Bloco profundo — `curva_abc.js`, `curva_abc_processo.js`, `utils.js`, `database.js`, `app.js` (revisão linha a linha completa via agente dedicado)
- [x] Bloco profundo — `main.js` (8.349 linhas, revisão linha a linha completa via agente dedicado)
- [x] Bloco profundo — scripts inline de `index.html`, validação de formulários, CSS órfão em `style.css`
- [x] **Segunda rodada de correções (2026-07-26)** — XSS em `relatorio.js`/`whatsapp.js`/`admin.js` (injeção via onclick), rich-text XSS em `curva_abc.js`/`curva_abc_processo.js` (DOMPurify), código morto duplicado em `main.js`/`dashboard.js`, corrupção de acentuação em rótulos de PDF, `console.log` de sessão, performance (querySelector em loop, thead). Ver seção 7.
- [ ] Bloco 6 — Compatibilidade cross-browser/mobile — requer teste manual em navegador (não é possível auditar apenas por leitura de código)
- [ ] Consolidação final e plano de correção → em andamento, ver seção 7

**Próximo passo:** validar manualmente no navegador (login, cadastro, comentários com formatação rica, geração de PDF/relatórios, painel Admin) antes de commitar. O Bloco 6 (compatibilidade) permanece pendente de teste manual. O backdoor `promoteAdmin99030487` foi mantido por decisão explícita do usuário (2026-07-26).

**Duas rodadas de correções já aplicadas** (branch `revisao/index-html`, ainda não commitadas) — ver seções 4 e 7 para o detalhamento completo.

---

## 1. ÁRVORE DE DEPENDÊNCIAS DO index.html

```
index.html
├── CSS externo (CDN)
│   ├── Google Fonts (Montserrat) — fonts.googleapis.com
│   ├── bootstrap@5.3.3/dist/css/bootstrap.min.css — cdn.jsdelivr.net
│   └── bootstrap-icons@1.11.3 — cdn.jsdelivr.net
├── CSS local
│   └── style.css (3.949 linhas) — link rel="stylesheet" (linha 19)
├── Imagem preload
│   └── fachada.jpg (rel="preload")
├── Imagens inline
│   └── brasao.png (logo, landing page)
├── JS externo (CDN) — carregados no <head>
│   ├── @supabase/supabase-js@2                          (SEM defer — bloqueante)
│   ├── plotly-2.35.2.min.js                              (defer)
│   ├── jspdf 2.5.1                                        (defer)
│   ├── jspdf-autotable 3.5.31                             (defer)
│   ├── html2pdf.js 0.10.1                                 (defer)
│   ├── docx@7.1.0                                         (defer)
│   ├── pdf-lib@1.17.1                                     (defer)
│   ├── xlsx (SheetJS) latest                              (defer)
│   ├── exceljs@4.4.0                                      (defer)
│   └── sweetalert2@11                                     (defer)
├── JS externo (CDN) — no fim do <body>, linha 3886
│   └── bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js   (SEM defer/async)
└── JS local — no fim do <body>, linhas 4392-4406, NESTA ORDEM:
    ├── config.js              (10 linhas — credenciais/URLs globais)
    ├── whatsapp.js             (992 linhas)
    ├── relatorio.js            (913 linhas)
    ├── utils.js                (209 linhas)
    ├── database.js             (57 linhas)
    ├── dashboard.js            (690 linhas)
    ├── admin.js                (755 linhas)
    ├── curva_abc.js            (1.211 linhas)
    ├── curva_abc_processo.js   (231 linhas)
    ├── app.js                  (121 linhas)
    └── main.js                 (8.349 linhas) ← maior arquivo, carregado por último
```

**Observação estrutural:** os `<script>` locais (linhas 4392-4406) estão aninhados dentro de
vários `<div>` no fim do `<body>`, com indentação profunda incomum — indício de que foram
inseridos ali via edição rápida, não no local "canônico" de fechamento do body. Não é um bug
funcional (scripts fora de tags de bloco ainda são movidos pelo parser HTML para o body), mas é
um sinal de organização a observar. **Requer confirmação** antes de qualquer reorganização.

Nenhum dos scripts locais usa `defer`/`async` — todos bloqueiam o parsing até serem baixados e
executados, na ordem listada acima (que também define suas dependências implícitas entre si,
já que não são módulos ES — todos populam o escopo global `window`).

## 2. OUTROS PONTOS DE ENTRADA QUE COMPARTILHAM ARQUIVOS

| Arquivo | Compartilha com index.html | Risco de efeito colateral |
|---|---|---|
| `cronograma.html` | `style.css`, `config.js`, `database.js`, `bootstrap` CDN, Google Fonts | **Alto** — qualquer alteração em `style.css`, `config.js` ou `database.js` afeta as duas páginas. `cronograma.html` faz `window.location.href` de/para `index.html` (navegação cruzada, inclusive com querystring `?pane=pane-reuniao`). |
| `layout/GECOPE - LAYOUT.html` | Nenhum (arquivo standalone, mockup visual, 10MB, sem `<script src>` para os JS do sistema) | Nenhum — fora do escopo, não referencia os arquivos elegíveis |
| `Novas opções de layout GECOPE/Processo - Novas Opções.dc.html` | Nenhum (mockup estático) | Nenhum — fora do escopo |

**Conclusão do Passo 0:** os únicos arquivos "elegíveis" para alteração (usados efetivamente por
`index.html`) são `style.css`, `config.js`, `whatsapp.js`, `relatorio.js`, `utils.js`,
`database.js`, `dashboard.js`, `admin.js`, `curva_abc.js`, `curva_abc_processo.js`, `app.js` e
`main.js`. Destes, **`style.css`, `config.js` e `database.js` também são usados por
`cronograma.html`** — qualquer correção nesses três arquivos deve ser testada nas duas páginas.

---

## 3. ACHADOS

### BLOCO 1 — Segredos e credenciais expostas

| Sev. | Arquivo | Linha | Problema | Impacto |
|---|---|---|---|---|
| **Crítico** | `config.js` | 7 | `window.EVO_API_KEY = 'sopgecope2026'` — chave da Evolution API (WhatsApp) hardcoded e enviada ao navegador de **qualquer** visitante, mesmo antes do login (script carrega no fim do body, mas antes de qualquer gate de autenticação real). Confirmado em 5 commits do histórico git, nunca rotacionada. | Qualquer pessoa com o DevTools aberto lê `window.EVO_API_KEY` e pode enviar mensagens/ler conversas via API do WhatsApp da instituição diretamente (`whatsapp.js:481,759,785,904,937,970` fazem `fetch` direto para `EVO_API_URL` com essa chave). O projeto já tem um proxy dedicado para isso (`evo-proxy/`, com variáveis de ambiente server-side) que **não está sendo usado** pelo front-end. |
| **Crítico** | `admin.js` | 5-6, 50-56, 66-75 | `SENHA_MESTRA = "sop2026"` hardcoded em texto plano no JS público. Pior: o "modo Administrador" é controlado só por `sessionStorage.setItem('is_admin_gecope','true')` — qualquer usuário pode digitar isso no console do navegador **sem saber a senha** e o front-end passa a se comportar como admin (`document.body.classList.add('is-admin')`). | Bypass total de autenticação client-side. Se qualquer ação restrita (aprovar usuário, editar processo, excluir dados) depender só dessa flag/classe e não de checagem server-side (RLS do Supabase), é uma falha crítica de autorização. **Requer confirmação**: preciso verificar as policies RLS do Supabase para saber se o backend também valida, ou se o cliente é a única barreira. |
| **Médio** | `config.js` | 2-3 | `SUPABASE_URL`/`SUPABASE_KEY` (anon key) expostos no front-end. Isso é o padrão esperado do Supabase (a anon key é pública por design), **mas só é seguro se houver Row Level Security (RLS) habilitado e restritivo em todas as tabelas.** | Sem RLS adequado, a anon key sozinha permite leitura/escrita irrestrita no banco. **Requer confirmação**: preciso checar as policies no painel Supabase (fora do escopo de arquivos do repo). |
| Baixo | `admin.js` | 693 | `is_admin_gecope: sessionStorage.getItem('is_admin_gecope')` é enviado/lido de sessionStorage e usado como se fosse dado confiável em outro ponto do fluxo. | Reforça o padrão acima — decisões de permissão remontadas a partir de estado 100% client-side. |

### BLOCO 2 — XSS, eval, links externos, sessão

| Sev. | Arquivo | Linha | Problema | Impacto |
|---|---|---|---|---|
| **Alto** | `main.js` | 5390, 5831 | `sel.innerHTML = `<option value="${currentUserName}"...` — `currentUserName` vem de `sessionStorage.getItem('sop_user_name')` (linha 5389/5980), que por sua vez é populado a partir do nome digitado no cadastro ("Primeiro acesso", overlay de login em `index.html`). Se o campo de nome não for sanitizado/validado no cadastro, um usuário pode registrar um nome como `<img src=x onerror=...>` e obter XSS armazenado quando esse nome for renderizado via `innerHTML` em qualquer tela. | XSS armazenado potencial. **Requer confirmação**: localizar a rotina de cadastro de usuário e verificar se há sanitização/validação de caracteres no campo nome antes de gravar no Supabase. |
| Médio | 186 ocorrências de `.innerHTML =` em `main.js` (114), `admin.js` (14), `whatsapp.js` (13), `curva_abc.js` (18), `relatorio.js` (9), `dashboard.js` (10), `curva_abc_processo.js` (6), `app.js` (2) | várias | Uso extensivo de `innerHTML` para renderizar conteúdo, incluindo mensagens de erro que ecoam `err.message`/`error.message` diretamente (ex.: `main.js:1551,1837,1847,5054,6020,6187,7133,7675`). Na maioria dos casos são strings fixas ou mensagens de erro do próprio sistema (baixo risco), mas o padrão geral deveria usar `textContent` sempre que o valor não é HTML controlado. | Risco baixo-médio disperso; o item de maior risco real é o `currentUserName` acima. Cada ocorrência precisa ser avaliada individualmente antes de trocar por `textContent` (posso quebrar formatação HTML intencional). |
| Baixo | `index.html` | 685-687, 689-691, 3086-3090 | 3 links `target="_blank"` sem `rel="noopener noreferrer"` (para o painel da Evolution API e para sindusconce.com.br). | Tabnabbing — a aba aberta tem acesso a `window.opener`. Correção simples e seguríssima (adicionar atributo `rel`), baixo risco de quebrar algo. |
| — | — | — | `eval()`, `new Function()` e `setTimeout`/`setInterval` com string **não foram encontrados** em nenhum arquivo JS do projeto. | Ponto positivo — sem essa classe de vulnerabilidade. |
| — | — | — | Nenhum ID duplicado encontrado em `index.html` via varredura de todos os atributos `id="..."`. | Ponto positivo. |

### BLOCO 3/4/5 — Bugs, código morto e desempenho (achados iniciais por amostragem)

| Sev. | Arquivo | Linha | Problema | Impacto |
|---|---|---|---|---|
| Médio | `index.html` | 22 | `<script src=".../@supabase/supabase-js@2">` no `<head>` **sem** `defer`, enquanto todas as outras 9 libs do `<head>` usam `defer`. É o único script bloqueante do carregamento inicial da página. | Atraso perceptível no First Paint/overlay de login, especialmente em conexões lentas. Precisa confirmar se algum código depende de `supabase` estar pronto sincronamente antes do `DOMContentLoaded` (senão dá para adicionar `defer`). |
| Médio | `index.html` | 3885-3886 | `bootstrap.bundle.min.js` carregado sem `defer`/`async` no fim do body, antes dos 11 scripts locais — os scripts locais (`admin.js`, `main.js` etc.) usam `bootstrap.Modal` então dependem dessa ordem síncrona. Não é bug, mas trava o parsing nesse ponto. | Baixo impacto (já está no fim do body), mas é outra oportunidade de otimização condicionada a teste. |
| Estrutural | `index.html` | 4392-4406 | Os 11 `<script src>` locais estão aninhados dentro de vários `<div>`, com indentação muito profunda — sinal de inserção "no meio do meio" do HTML em vez de antes do `</body>`. Funciona porque o parser HTML realoca `<script>` para fora de elementos inline automaticamement em alguns casos, mas é frágil e difícil de manter. | **Requer confirmação** antes de mover — risco de efeito colateral em ordem de carregamento se algo depender da posição atual. |
| Baixo | `index.html` | global | Apenas 1 `<img>` na página inteira e nenhum `loading="lazy"`. | Impacto real baixo (poucas imagens no HTML — a maioria do visual vem de CSS/background), mas fácil de aplicar no único caso encontrado. |
| A confirmar | vários | — | 9 listeners de `DOMContentLoaded` espalhados entre `admin.js` (2), `main.js` (6), `whatsapp.js` (1). Pode ser intencional (um por módulo), mas precisa checar se não há inicialização duplicada de UI/listeners quando múltiplos handlers tocam o mesmo elemento. | Pendente de revisão linha a linha — não avaliado em profundidade nesta sessão. |
| Pendente | `main.js`, `curva_abc.js`, `dashboard.js`, `relatorio.js` | — | Ainda não foram revisados linha a linha para: funções/variáveis não usadas, blocos comentados obsoletos, `console.log` remanescente (existem ocorrências mas não classifiquei quais são debug esquecido vs. logging intencional), CSS órfão em `style.css` (3.949 linhas), debounce/throttle em buscas e inputs. | Fica para a próxima sessão/bloco, listado em PROGRESSO acima. |

### BLOCO PROFUNDO — admin.js, whatsapp.js, relatorio.js, dashboard.js (revisão linha a linha)

| Sev. | Arquivo | Linha | Problema | Impacto |
|---|---|---|---|---|
| **Crítico** | `admin.js:711-723,752` **e duplicado em** `main.js:4738-4768` | — | Função `promoteAdmin99030487()` promove automaticamente a admin o usuário com e-mail hardcoded `99030487@gecope.app`, disparada via `setTimeout(promoteAdmin99030487, 500)` dentro de `DOMContentLoaded` — **roda sozinha em toda carga de página**, sem senha nem confirmação, fazendo `UPDATE` direto na tabela `app_users` do Supabase. Está **duplicada palavra-por-palavra em `admin.js` e `main.js`**, então dispara duas vezes por carregamento. | Backdoor de escalação de privilégio embutido no código público: qualquer pessoa que descubra/registre essa matrícula (`99030487`) vira admin automaticamente. A proteção real depende 100% de RLS no Supabase (não auditado). **Este é o achado mais grave da auditoria até agora — mais grave que a senha mestra hardcoded, porque não exige nem digitar senha nenhuma.** |
| **Alto** | `admin.js:166-224,253-277` | — | `tr.innerHTML` monta linhas da tabela de usuários interpolando `u.email`, `u.matricula`, `nomeComp`, `u.telefone_whatsapp` sem escaping, inclusive **dentro de atributos** `onclick="aprovarUsuario('${u.email}', ...)"` (linha 182) e `excluirUsuario('${u.email}')` (linhas 185, 270). Esses valores vêm do autocadastro (controlável por qualquer usuário). | E-mail/nome com aspas simples quebra o atributo `onclick` e injeta JS arbitrário — executado no navegador do **administrador** ao abrir a lista de usuários pendentes. XSS armazenado com escalonamento de privilégio via painel admin. |
| **Alto** | `relatorio.js:213-316` (uso em 63,112,124,181,189,237) | — | `container.innerHTML` interpola `descricao`, `item.descricao`, `item.origem`, `f.nome` (fornecedor) e `r.descInsumo` — dados de composições gravadas por qualquer usuário autenticado — sem `escapeHTML`. | XSS armazenado disparado sempre que alguém abre/imprime o relatório dessa composição, atingindo qualquer usuário incluindo admins. |
| **Alto** | `whatsapp.js:206-208` | — | `data-name="${u.name}" data-email="${u.email}"` sem escaping, mesmo campo do autocadastro. | Mesmo vetor de XSS, na tela de disparo manual de WhatsApp. |
| **Alto** | `dashboard.js:1-261` vs `262-690` | — | Duas IIFEs completas definindo as **mesmas funções** (`getSelectedValues`, `renderMultiSelectUI`, `fillSelect`, `populateFinanceiroFilters`, etc.) em `window.*`; a segunda sobrescreve a primeira. **As linhas 1-261 (~38% do arquivo) são código morto** que ainda executa `getElementById` e monta objetos à toa em toda carga de página. | Risco de manutenção (alguém edita a cópia morta achando que corrigiu o bug) + peso desnecessário. |
| Médio | `admin.js:371-376` | — | No `catch` de `salvarEdicaoUsuario`, um `querySelector` é refeito sem checar null antes de `.innerHTML =`; se falhar, gera exceção **dentro do próprio catch**. | Falha não tratada em cascata. |
| Médio | `relatorio.js:21-25` | — | `window.open('', '_blank')` sem checar se o retorno é `null` (bloqueio de pop-up do navegador); função sem try/catch. | Impressão de relatório quebra silenciosamente quando o navegador bloqueia pop-up, sem feedback ao usuário. |
| Médio | `dashboard.js:495-514,642-660` | — | `renderKPIsFinanceiro` faz 9 `getElementById` sem null-check (diferente de `renderContadorFinanceiro`, que checa), dentro de `updateFinanceiro()` sem try/catch. | Se um único id de KPI mudar/faltar no HTML, todo o `updateFinanceiro()` para no meio, sem log de erro. |
| Médio | `whatsapp.js:1-23` | — | `notificarAtualizacaoTabelas` lê 3 elementos do DOM sem checar null, função `async` sem try/catch. | Promise rejeitada sem handler (falha silenciosa). |
| Médio | `admin.js:711-723` vs `main.js:4738-4768` | — | (mesmo achado do backdoor acima, listado aqui pela ótica de duplicação) — mesma query/gravação Supabase disparada 2x por carga de página. | Efeito colateral duplicado, além do risco de segurança já descrito. |
| Baixo | `admin.js:744-745` | — | `window.diagnosePendingFetch = diagnosePendingFetch;` atribuído duas vezes seguidas. | Código morto/resíduo de edição. |
| Baixo | `admin.js:281` | — | Mensagem de erro usa `colspan="5"` mas a tabela tem 7 colunas. | Desalinhamento visual da mensagem de erro. |
| Baixo | `whatsapp.js:440,522-530` | — | `lastNotificationCache` (Map de deduplicação) nunca é limpo/podado. | Vazamento de memória lento durante a sessão. |
| Baixo | `whatsapp.js` (fetches ~479,757,783,903,933,968) | — | Chamadas à Evolution API sem timeout/`AbortController`. | Spinner/texto "Consultando..." pode ficar pendurado indefinidamente se a API travar. |
| Baixo | `whatsapp.js` (arquivo inteiro) | — | Não encapsulado em IIFE (diferente de `admin.js`/`dashboard.js`); funções e variáveis no escopo global. | Risco de colisão de nomes com outros scripts. |
| Baixo | `dashboard.js:340-346` | — | Filtro de busca do multiselect roda sem debounce a cada tecla. | Escala mal se as listas crescerem (impacto baixo hoje). |
| Baixo | `relatorio.js:46-317` vs `319-531` | — | Lógica de agrupamento/BDI/desconto/preço final reimplementada de forma independente na renderização HTML e na exportação DOCX. | Risco de divergência futura entre o que a tela mostra e o que sai no DOCX quando uma regra de negócio mudar. |

**Resumo do bloco:** o achado mais grave desta rodada é o **backdoor de auto-promoção a admin** (`promoteAdmin99030487`), que roda sozinho a cada carregamento de página e está duplicado em dois arquivos. Em seguida, os quatro arquivos compartilham um padrão recorrente de `innerHTML` sem sanitização para dados de autocadastro/composições — só `dashboard.js` usa `escapeHTML` de forma consistente, e mesmo assim tem ~38% de código morto por duplicação de IIFE.

### BLOCO PROFUNDO — curva_abc.js, curva_abc_processo.js, utils.js, database.js, app.js (revisão linha a linha)

| Sev. | Arquivo | Linha | Problema | Impacto |
|---|---|---|---|---|
| **Alto** | `curva_abc.js:1019,544` e `curva_abc_processo.js:159` | — | Campo `comentario` (rich-text de um editor `contentEditable`) é injetado via `innerHTML`/`document.write` **sem sanitização**, diferente de `conta`/`desc`/`descricao` que sempre passam por `esc()`. Em `curva_abc_processo.js:159` é ainda pior: abre em `window.open('_blank')` + `document.write` — janela de mesma origem, com acesso a `window.opener`, `localStorage` e sessão. | XSS armazenado: qualquer HTML na coluna `comentario` de `curva_abc_itens` executa com privilégios totais para quem visualizar o item ou relatório. |
| Alto | `database.js:31-55` | — | `initSupabaseClient()` não dá nenhum feedback ao usuário se falhar (CDN bloqueado, ad-blocker etc.) — `sbClient` fica `null` para sempre, sem erro visível. | Toda chamada a `sbClient.from(...)` em `curva_abc.js`, `curva_abc_processo.js` e `main.js` lança `TypeError` silencioso; usuário só vê a tela travada sem explicação. |
| Médio | `database.js:55` | — | `window.addEventListener('load', initSupabaseClient)` chama a função sem try/catch (diferente da chamada síncrona da linha 54, que tem). | Exceção não tratada se `createClient()` falhar durante o evento `load`. |
| Médio | `curva_abc_processo.js:82` | — | `onclick="excluirVersaoCurvaAbc(' + v.id + ', ' + numero + ')"` insere `numero` (string zero-padded, ex. `"01"`) sem aspas no atributo — vira literal `01` (sintaxe octal legada no JS inline). Funciona hoje, mas frágil. | Quebraria com `SyntaxError` em modo estrito; deveria ir entre aspas como string. |
| Médio | `utils.js:182,184` | — | `quantile()` e `topN()` nunca são chamados em nenhum outro arquivo (confirmado por grep). `dashboard.js` tem cópia própria idêntica de `quantile` (linha 241). | Código morto duplicado — a versão de `utils.js` nunca é usada. |
| Baixo | `database.js:5-21` vs `main.js:462-478` | — | `cookieGuardStorage` (lógica de sessão via cookie) duplicado quase identicamente em `main.js`, que nunca chama `createClient` nem usa essa constante. | Código morto em `main.js`; risco de a cópia órfã ficar desatualizada se a lógica for corrigida só em `database.js`. |
| Baixo | `database.js:43-46` | — | `invokeFunction`, exposto em `window.invokeFunction`, sem nenhuma chamada em todo o repositório. | Código morto. |
| Baixo | `app.js:39-40` | — | `document.getElementById('btn-buscar-incc')` e `.innerHTML` usados sem checagem de null, **fora** do bloco try/catch (que só começa na linha 44). | `TypeError` não tratado se o botão não existir no DOM. |
| Baixo | `app.js:12-13` | — | Mesmo padrão: `getElementById('mercado-data-ini'/'mercado-data-fin').value` sem checagem de null. | Mesmo risco acima. |
| Baixo | `curva_abc.js:51` | — | `function erro(msg)` — a própria função central de exibição de erros do módulo — não checa se `#cv-err` existe. | Se o elemento faltar, mascara a mensagem de erro original atrás de outro `TypeError`. |
| Baixo | `curva_abc.js:926-965` (amostra) | — | Vários `$(id)`/`getElementById` sem checagem de null antes de `addEventListener` (a maioria tem `if (elemento)`, mas nem todos). | Alteração futura de um `id` no HTML pode interromper a IIFE inteira sem log claro. |
| Baixo | `curva_abc.js:955-960` | — | `#cv-qtdCustom` tem dois listeners (`change` e `input`) chamando `calcular()` — redundante. | Pode recalcular a curva duas vezes para a mesma interação. |
| Baixo | `app.js:100` | — | `console.log('[INCC] Valores normalizados e atribuídos:', ...)`. | Log de debug esquecido em produção. |
| Baixo | `curva_abc.js:134-143` | — | Loop aninhado O(colunas×linhas) em `processarAoa` para detectar colunas numéricas, a cada upload de planilha, sem cache. | Pode ficar perceptível em planilhas muito grandes (não crítico, roda 1x por upload). |
| Baixo | `app.js` (arquivo inteiro) | — | Todas as linhas com ~76 espaços de indentação — claramente extraído de um `<script>` inline e nunca reformatado. | Prejudica leitura/diffs/manutenção, sem impacto funcional. |
| — | `app.js:55-56` | — | Reutiliza a `SUPABASE_KEY` (anon key pública, já reportada) num `fetch` manual para Edge Function — mesmo padrão do resto do sistema, não é exposição nova. | Informativo, sem ação nova necessária. |
| — | — | — | Nenhum segredo novo hardcoded encontrado nestes 5 arquivos. Nenhuma duplicação de `<script src>`. | Ponto positivo. |

**Resumo do bloco:** os 5 arquivos são bem comentados e funcionalmente coerentes, mas o módulo Curva ABC é frágil a mudanças de HTML por falta de null-checks sistemáticos — inclusive na própria função `erro()`. O achado mais sério é o **XSS armazenado via campo `comentario`**, que quebra a política de escape usada no resto do código (`esc()`). `database.js` funciona no caminho feliz mas não sinaliza falha de inicialização do Supabase, o que pode gerar falhas silenciosas difíceis de diagnosticar em produção.

### BLOCO PROFUNDO — main.js (revisão linha a linha completa, 8.349 linhas)

| Sev. | Linha | Problema | Impacto |
|---|---|---|---|
| **Crítico** | 5397-5402, 5839-5844 | `chatContainer.innerHTML = comentarios.map(c => ...${c.autor}...${c.mensagem}...)` sem `escapeHTML()` nos modais "Adicionar Comentário" (orçamentos e composições). | XSS armazenado: qualquer usuário autenticado (inclusive papel `fiscal`) grava `<img src=x onerror=...>` no campo mensagem/autor e o payload executa no navegador de **qualquer usuário, inclusive admin**, que abrir o comentário — podendo ler `sessionStorage` (usado como mecanismo de auth) e agir como esse usuário. |
| **Crítico** | 5170-5220 (orçamentos), 6107-6123 (composições) | Os mesmos campos `c.autor`/`c.mensagem`/`c.resp_admin` são renderizados sem escape **na listagem em acordeão**, não só no modal — o payload dispara para qualquer usuário que apenas **abra a aba** Orçamentos/Composições, sem precisar clicar em nada. | Superfície de ataque bem maior que um XSS de modal isolado — dispara passivamente. |
| Alto | 5238, 5242-5243, 6142-6147 | `obra.nome_obra`, `obra.descricao`, `obra.subcategoria` (texto livre do formulário de cadastro, digitável por qualquer usuário) inseridos via `innerHTML` sem escape na listagem. | Mesmo vetor de XSS armazenado, ativado só por criar um item com nome malicioso. |
| Alto | 420-435 vs 5634-5636 | `abrirModalAtender` declarada duas vezes; por *hoisting* a segunda (delega para `abrirModalDecisao`) vence — a primeira é código morto inalcançável. | Manutenção futura na versão de cima não terá efeito nenhum, sem aviso do runtime. |
| Alto | 437-451 vs 5642-5644 | Mesmo problema para `abrirModalRecusar`. | Idem acima. |
| Alto | 5619-5621 vs 5915-5923 | `deletarComposicao` duplicada — a versão "boa" (reusa `deletarRegistroGenerico`, padroniza cursor/callback) nunca roda; quem executa é a versão solta com `confirm()` próprio. | Risco concreto de alguém corrigir um bug na função errada. |
| Médio | 3350-3391 | `.then()` sem `.catch()` em `getMetaDate` (update de `processos`). | Falha de rede não tratada: meta não é salva, histórico não é gravado, usuário acredita que salvou. |
| Médio | 2510-2513 | Mesmo padrão em `abrirModalChecklistAditivo`. | Modal "trava" sem mensagem de erro. |
| Médio | 6592-6730 (`executarBuscaItemComposicao`) | Função `async` sem `try/catch` (diferente da irmã `executarBuscaTabela`, linha 7073, que é protegida). | Falha de rede deixa spinner "Buscando..." preso indefinidamente, sem erro. |
| Médio | 7340-7341, 7500-7501, 7614, 7626, 7669, 8027, 8042, 8072 | Corrupção de acentuação em rótulos exibidos na tela **e em PDFs oficiais gerados pelo sistema** (jsPDF): "CDIGO", "VERSO", "SUPERINTENDNCIA DE OBRAS PBLICAS", "PREO TOTAL", "SERVIO". | Documentos oficiais impressos/baixados por fiscais e enviados a terceiros saem com erros de grafia visíveis. |
| Baixo | 290-295 vs 3098-3109 | Comentário do código diz que a aba Composições tem cache ("só carrega na primeira visita"), mas o listener `shown.bs.tab` não checa a flag `_composicoesCarregadas` — recarrega do banco toda vez que a aba é reaberta. | Comportamento diverge do que o próprio código documenta; consultas redundantes ao Supabase. |
| Baixo | 1715-1717, 5019, 5976, 6594, 7077 (padrão repetido) | `document.getElementById(id).value` encadeado sem checar null. | Funciona hoje (HTML estático), mas qualquer renomeação de `id` quebra com `TypeError` não tratado. |
| Baixo | 4724-4737 | 4 cabeçalhos de comentário empilhados sem código entre eles (funções removidas, comentários esquecidos); um tem `/* ----` aninhado indevidamente. | Código morto / resíduo de edição, confunde leitura. |
| Baixo | 5646-5652 | Mesmo bloco de comentário `/* LÓGICA DAS NOVAS ABAS... */` duplicado, um logo após o outro. | Resíduo de merge/reescrita. |
| Baixo | 4962-4964 | 3 cabeçalhos sucessivos para a mesma função ("CORRIGIDO VFINAL", "VERSÃO FINAL CORRIGIDA", "COM ORDENAÇÃO NUMÉRICA CORRIGIDA"). | Evidência de múltiplas reescritas sem limpar comentários antigos. |
| Baixo | 409-415 + ~30 ocorrências | `console.log`/`console.info` viram no-op quando `IS_PROD=true`, mas dezenas de `console.log('[DEBUG]...')` continuam no código, incluindo **dump de dados de sessão do usuário logado** (`sop_user`, `sop_role`) nas linhas 3145-3146 e no fluxo de login (4348-4399). | Inofensivo hoje (`IS_PROD=true` desativa), mas risco real se `IS_PROD` for revertido para depuração em produção sem revisar esses logs antes — vazaria dados de sessão no console. |
| Médio (perf) | 789, 851, 4046 | `document.querySelector('tr[data-numero="..."]')` (seletor de atributo, sem índice) executado **1x por processo dentro de `.map(async...)`**, em `atualizarTabelaSuite` (a cada refresh) e `varrerRiscoDiligenciaSegundoPlano` (a cada 5 min, para todos os processos "APROVADO"). | Varredura O(linhas×nós) repetida constantemente; poderia ser um `Map` montado 1x via `querySelectorAll`+`data-numero`. |
| Baixo (perf) | 3836 | `window.abrirModalMeta` (função `async`) é recriada a cada chamada de `updateReuniao()`, inclusive a cada tecla na busca (apesar do debounce de 300ms). | Churn de função desnecessário, sem motivo funcional. |
| Baixo (perf) | 3673-3680 | `<thead>` da tabela de processos reconstruído via `innerHTML` do zero em toda execução de `updateReuniao()`, mesmo quando só filtro/ordenação de linhas mudou. | Trabalho de DOM desnecessário repetido. |
| — | — | Funções como `fillSelect`, `updateFinanceiro`, `renderizarRelatorioSOP_HTML`, `verificarAdminSalvo` são usadas em `main.js` mas definidas em `dashboard.js`/`admin.js`/`relatorio.js` (carregados antes). Confirmado: **não é código órfão**, é acoplamento implícito via `window` global — frágil à ordem de carregamento dos `<script>`, mas funcional hoje. | Não alterar a ordem dos scripts em `index.html:4392-4406` sem revalidar todas essas dependências implícitas. |
| — | — | Nota positiva: debounce bem aplicado na maior parte das buscas (`mt.search`:4212, `orcamento-search`:5269, `comp-search`:6197, `busca-bdi`/`busca-desc`:4235-4236); listas grandes usam concatenação em string + 1 atribuição a `innerHTML`, não `+=` em loop. | Padrão correto, não requer correção. |

**Resumo do bloco:** `main.js` mostra sinais claros de várias rodadas de "correção rápida" sem limpeza — comentários de seção duplicados/órfãos, 3 funções redeclaradas onde a versão "boa" nunca executa por causa de hoisting, e corrupção de acentuação que vazou até para PDFs oficiais. Tratamento de erro é inconsistente: a maioria das funções assíncronas tem try/catch, mas fluxos críticos de escrita (meta, checklist, busca de item de composição) falham silenciosamente. **O risco mais sério é de segurança**: comentários de usuário e campos de texto livre são injetados via `innerHTML` sem sanitização em pelo menos 6 pontos das abas Orçamentos/Composições, visíveis a qualquer usuário que apenas abra a lista — combinado com a sessão fraca via `sessionStorage` já reportada, forma uma cadeia de XSS armazenado com potencial de comprometer sessões de administrador.

### BLOCO PROFUNDO — Scripts inline de `index.html` (revisão linha a linha completa)

**Achado prévio confirmado:** `index.html` tem apenas **2 blocos `<script>` inline** (sem `src=`) em toda a sua extensão — linhas **143-170** e **172-185**. Todos os demais 24 `<script>` do arquivo (linhas 22-32, 3885-3886, 4392-4406) são `<script src="...">`, sem código solto. Isso é bem menos do que o volume que se poderia esperar de um HTML de 4410 linhas, e finaliza a apuração pendente sobre "lógica de negócio solta no HTML" — praticamente não existe.

| Sev. | Arquivo | Linha | Problema | Impacto |
|---|---|---|---|---|
| Médio | `index.html:148-157` vs `main.js:24-36` | — | A IIFE de "anti-flash de login" (`sessionStorage.getItem('sop_role')` → esconde `#landingOverlay`, remove `login-active`) duplica manualmente o branch `show=false` da função `toggleLanding()` de `main.js`. São duas implementações independentes da mesma regra — se `toggleLanding()` for corrigida/alterada (ex.: mudar o nome da role "guest", adicionar um terceiro estado), o trecho inline em `index.html` não é atualizado junto, pois nada os mantém sincronizados. | Risco de divergência silenciosa: um ajuste de regra de sessão feito só em `main.js` não se reflete no comportamento "antes do main.js carregar", podendo reintroduzir o próprio flash de login que o código foi escrito para evitar. |
| Baixo (informativo) | `index.html:151` | — | Condição `if (role && role !== 'guest')`. Em todo o restante do sistema (`main.js`), `sessionStorage.setItem('sop_role', ...)` só grava os valores reais de perfil (`admin`, `gerente`, `fiscal`, `externo`, `pending` — linha 4371, valor vindo de `profile.data.role`). A string literal `'guest'` **nunca é gravada** no `sessionStorage`; ela só aparece como fallback de leitura (`... || 'guest'`) em dezenas de outros pontos. Ou seja, essa checagem defensiva é, na prática, sempre `true` quando `role` existe — o `!== 'guest'` não filtra nada hoje. | Nenhum impacto funcional atual (código morto lógico, não código morto de execução). Vale só como nota para quem for mexer nessa condição futuramente, achando que ela cobre um caso real. |
| — (ponto positivo) | `index.html:143-185` (os 2 blocos) | — | Nenhum `innerHTML`/`document.write` com dado externo; ambos usam apenas `style.display`, `classList` e `src` de imagem. Ambos rodam dentro de IIFE (sem vazar variável/função global, logo sem colisão possível com `main.js`/`admin.js`/etc.). O bloco de sessão tem `try/catch` (silencioso, mas presente); o bloco de fade da imagem trata `onload` **e** `onerror` do `Image()`, então nunca fica "pendurado" sem callback. | Nenhuma correção necessária — os dois blocos são exemplos legítimos de "flash-of-unstyled-content prevention" que precisam mesmo rodar inline e de forma síncrona (antes do parser chegar em Bootstrap/Supabase/`main.js`, carregados ~4200 linhas depois). Movê-los para um `.js` externo com `defer` reintroduziria o flash que eles existem para evitar — **não recomendo mexer nisso**. |

**Resumo do bloco:** `index.html` não tem lógica de negócio relevante solta em `<script>` inline — os únicos dois blocos existentes são micro-scripts de UX (anti-flash de tema e de login) corretamente justificados por comentário e corretamente isolados em IIFE, sem `innerHTML`/`document.write` e com tratamento de erro adequado ao que fazem. O único ponto real de atenção é a **duplicação de regra** entre a IIFE de `index.html:148-157` e `toggleLanding()` em `main.js`, que é um risco de manutenção (drift), não uma vulnerabilidade. Não há colisão de nomes com `main.js`/`admin.js`/etc., nem código comentado/morto dentro dos blocos.

### BLOCO PROFUNDO — Validação de formulários em `index.html`

`index.html` tem **23 `<form>`**. Confirmação central: **o atributo `pattern=` não é usado nenhuma vez em todo o arquivo** (0 ocorrências) — a única validação client-side existente é `required`, `type="email"` (só em 2 campos: `reg-email` e `edit-user-email`), `type="tel"` (sem validação real de formato) e `maxlength` (só em 3 campos, nenhum deles em nome/matrícula). **Não existe `minlength=` em nenhum lugar** (nem no campo de senha de cadastro).

| Sev. | Arquivo | Linha | Problema | Impacto |
|---|---|---|---|---|
| **Alto (confirmação do achado pendente)** | `index.html:104-110` (`reg-nome`, `reg-sobrenome`, `reg-matricula`) + `main.js:4249-4324` (`signUpRequest`) | — | **Confirmado, não refutado**: a rotina de "Primeiro acesso" não faz **nenhuma** sanitização de caracteres. `reg-nome`/`reg-sobrenome`/`reg-matricula` só passam por `.trim()` (`main.js:272-275`) antes de `sbClient.auth.signUp()` e `INSERT`/`UPDATE` em `app_users` (linhas 4282-4310). Não há regex, não há `maxlength` no HTML para esses 3 campos, e nenhuma função tipo `escapeHTML`/`sanitize` é chamada no caminho. O valor vira `sop_user_name` (linha 4384) e alimenta diretamente os pontos de XSS armazenado já catalogados em `main.js:5390/5831`. | Fecha o "requer confirmação" da seção 5 do relatório: a cadeia completa é **cadastro sem sanitização → `sessionStorage.sop_user_name` → `innerHTML` sem escape** em pelo menos 4 telas (Orçamentos, Composições — já reportadas — mais as duas novas abaixo). |
| **Alto (novo)** | `main.js:1316-1332` (`carregarAtividades`, alvo `#full-activities-list`) e `main.js:1365-1387` (`carregarAtividadesResumoHome`, alvo `#home-activities-list`, painel "Atividades Realizadas" da Home) | — | Instância **nova**, não coberta no bloco profundo de `main.js`: `at.usuario` e `at.descricao` (gravados por `registrarAtividade()`, linha 1252-1271, com `usuario: sessionStorage.getItem('sop_user_name')`) são interpolados via `innerHTML` **sem nenhum escape**, tanto na aba "Últimas Atividades" quanto no card de atividades recentes da Home — a mesma raiz do achado acima (nome não sanitizado no cadastro), mas em uma superfície de tela diferente das já reportadas. | XSS armazenado disparado só de abrir a Home ou a aba Atividades — não exige abrir nenhum modal/registro específico, então tem alcance ainda maior que os pontos já catalogados. |
| **Alto (novo, classe diferente: injeção de filtro, não XSS)** | `main.js:1303` e `main.js:1354` | — | `query.or(\`tipo.neq.PROCESSO,fiscal.eq.${userName}\`)`, onde `userName = sessionStorage.getItem('sop_user_name')` — o mesmo campo "nome" sem sanitização do cadastro é interpolado **sem escape** dentro da sintaxe de filtro do PostgREST (`.or()` usa vírgula como separador de condições e parênteses para agrupar). Um nome de cadastro contendo vírgula/parêntese pode alterar a lógica do filtro. Essa `.or()` é exatamente o mecanismo que restringe usuários com perfil `fiscal` a só verem atividades das próprias obras. | Um usuário `fiscal` (perfil não-admin) pode conseguir, só escolhendo um nome malicioso no cadastro, neutralizar a cláusula de restrição e enxergar atividades de outros fiscais/processos que não deveriam ser visíveis a ele — potencial **bypass do controle de acesso por perfil** implementado no cliente (client-side só, sem confirmação de RLS equivalente no servidor). Diferente das 3 chamadas semelhantes em `main.js:6613,6655,7117` (que interpolam `termo` de busca digitado na hora, sem ligação com controle de acesso — impacto bem menor). |
| Médio | `main.js:5409-5415` (`enviarComentarioOrcamento`) e `main.js:5852` região equivalente (`enviarComentarioComposicao`) | — | A única validação antes de gravar o comentário no Supabase é `if (!autor || !msg) { alert(...); return; }` — checagem de "não vazio", zero checagem de conteúdo. `autor` vem do `<select id="coment-fiscal">`, que é populado (linha 5387, já reportada) incluindo uma `<option>` com `currentUserName` cru. O textarea `MENSAGEM` (`index.html:2291-2293`, `4097-4099`) só tem `required` no HTML, sem `pattern`/`maxlength`/checagem de tags. | Reforça e localiza com precisão o achado já registrado de XSS via comentário: a única barreira entre o textarea livre e o `innerHTML` do histórico de comentários é "campo não pode estar vazio". |
| Baixo (positivo, contraste) | `index.html:116-117` (`reg-telefone`) + `main.js:278` | — | Diferente de nome/matrícula, o campo telefone **é** sanitizado: `telefone = elTel.value.replace(/\D/g, '')` remove tudo que não é dígito antes de gravar. `maxlength="15"` no HTML reforça o limite de tamanho. | Ponto positivo isolado — mostra que o padrão de sanitizar existe no código-base, só não foi aplicado aos campos de texto livre (nome/sobrenome/matrícula/mensagem). |
| Baixo (positivo, contraste) | `index.html:3673-3872` (`formCadastro`, Novo Processo) + `main.js:1714-1783` (`enviarParaPlanilha`) | — | Este é o único formulário do arquivo com validação robusta e redundante: `form.checkValidity()` + `reportValidity()`, depois uma segunda checagem manual de 8 campos obrigatórios, **regex rígida** `^\d{5}\.\d{6}\/\d{4}-\d{2}$` para o número do processo, checagem de duplicidade local (`window.allData`) e checagem de duplicidade no servidor antes de salvar. | Nenhuma ação necessária — é o exemplo a ser seguido nos demais formulários (comentários, cadastro de usuário) que hoje só têm `required`. |
| Baixo | `index.html:113` (`reg-senha`) | — | Sem `minlength=` no HTML e sem checagem de força de senha em `main.js` antes de `sbClient.auth.signUp()` — a única validação de senha é a que o Supabase Auth aplicar no servidor (padrão mínimo de 6 caracteres, não confirmável por leitura de arquivo). | Senhas fracas (`123456`) passam sem aviso ao usuário na hora do cadastro; não é possível avaliar a política real sem acesso ao painel Supabase (mesma ressalva já feita para RLS). |
| Baixo | `index.html:111`, `257` (`reg-email`, `edit-user-email`) | — | `type="email"` é a única validação de formato de e-mail em todo o sistema, e só nesses 2 campos — não há revalidação em JS (`main.js` só faz `.trim().toLowerCase()`, linha 4254/4330, sem checagem de formato). A validação HTML5 de e-mail é client-side e pode ser removida/burlada via DevTools sem que nenhuma camada JS pegue o erro antes do `INSERT`. | Baixo risco isolado (o Supabase Auth também valida formato de e-mail no `signUp`), mas reforça o padrão geral: quase toda validação do sistema é só "existe no HTML", sem espelho em JS nem confirmação server-side visível pelo repositório. |

**Resumo do bloco:** a rotina de cadastro ("Primeiro acesso") **não sanitiza nome, sobrenome nem matrícula** — confirma, com localização exata (`main.js:4249-4324`, campos `index.html:104-110`), o achado que estava pendente de confirmação na seção "REQUER CONFIRMAÇÃO" do relatório. Além de confirmar a cadeia de XSS já suspeitada, a investigação encontrou **duas superfícies novas** do mesmo problema: o painel de Atividades (Home e aba dedicada, `main.js:1316-1387`) renderiza o mesmo nome não sanitizado via `innerHTML`; e, mais grave por ser uma classe diferente de falha, **o mesmo campo "nome" é interpolado sem escape dentro de uma cláusula `.or()` do PostgREST** que implementa a restrição de visibilidade por perfil `fiscal` (`main.js:1303,1354`) — um vetor de bypass de controle de acesso, não apenas de XSS. Em contraste, telefone (regex de dígitos) e o cadastro de Processo (`formCadastro`) mostram que o time sabe validar bem quando quer — só não aplicou o mesmo cuidado aos campos de nome/comentário.

### BLOCO PROFUNDO — CSS órfão em `style.css`

Metodologia: extraídos **338 seletores de classe** e **101 candidatos a seletor de id** de `style.css` (os candidatos a id incluíam muitos falsos-positivos — cores hex como `#CED4DA` capturadas pela regex; filtrados para os **15 ids reais** usados como seletor CSS). Cada seletor de classe restante foi conferido por busca de substring no conteúdo concatenado de `index.html` + `cronograma.html` + todos os 11 `.js` do diretório raiz (cobrindo `class="..."`, `classList.*`, `getElementById`, `querySelector*`, e concatenação de template string).

**IDs:** todos os 15 ids reais definidos em `style.css` (`app-content`, `btn-send-direct-msg`, `cv-coment-fontsize`, `direct-msg-recipients`, `direct-msg-text`, `meetingFiscalSelect`, `meetingMetaSelect`, `meetingPrioritarioSelect`, `meetingSearch`, `meetingStatusSelect`, `modal-report-body`, `modalCvRelatorioComentarios`, `modalDetalheComposicao`, `pane-home`) estão em uso — **nenhum id órfão encontrado**.

**Classes:** 39 candidatas ficaram com zero ocorrência fora de `style.css`. Agrupadas por família/causa provável:

| Sev. | Arquivo | Linha | Problema | Impacto |
|---|---|---|---|---|
| Baixo | `style.css:3018,3026,3037,3045,3049,3162` (`.history-container`, `.history-item`, `.history-header`, `.history-msg`, `.history-link`, `.history-card-msg`) | — | Família completa de classes de um card de "histórico" antigo, sem nenhuma referência. O HTML/JS atual usa uma **outra** família com nomes parecidos mas diferentes: `.history-card-item`, `.history-card-header`, `.history-card-body`, `.history-collapse-box`, `.btn-history-anexo` (`main.js:5197-5225`, também definidas em `style.css:3125-3239`) — confirma que é uma renomeação de UI cujo CSS antigo não foi removido, e não uma classe gerada dinamicamente. | Peso morto no CSS (~6 regras completas); risco de alguém editar a versão errada achando que está ajustando o card de histórico real. |
| Baixo | `style.css:185,195,204,1580,221` (`.admin-stat-icon-wrapper`, `.admin-stat-label`, `.admin-stat-value`, `.admin-section-title`, `.admin-accordion-header`) | — | Resquício de uma versão anterior do painel de Administração. O HTML atual usa `.proc-kpi-card/.proc-kpi-icon/.proc-kpi-label/.proc-kpi-value` (`index.html:511-541`) para os cartões-indicador e `.admin-section-header` (não `.admin-accordion-header`) para os cabeçalhos de seção (`index.html:580,646`). | Mesma classe de risco acima — CSS de uma versão anterior do painel Admin nunca removido. |
| Baixo | `style.css:3110` (`body:not(.is-admin) .admin-tab`) | — | Regra referencia uma classe `.admin-tab` que não existe em nenhum elemento do HTML. O mecanismo real de ocultar abas restritas a admin usa o atributo `data-roles="admin"` nos `<li>` de `#dashboardTabs` (`index.html:364-367`), filtrado via JS — e, além disso, `#dashboardTabs` já está permanentemente oculto com a classe `d-none` (comentário na linha 348: "NAVEGAÇÃO REMOVIDA — Substituída por Tiles"). Regra duplamente morta. | Nenhum impacto visual (a regra nunca encontra alvo), mas confunde quem for ler o CSS achando que existe um controle de visibilidade por classe que na verdade não é usado. |
| Baixo | `style.css:269,274,279,284,289` (`.icon-bg-blue/green/orange/red/slate`) | — | Outra família de cores de ícone sem uso — o HTML aplica cor via classes mais curtas `bg-green`/`bg-blue`/`bg-orange`/`bg-slate`/`bg-purple`/`bg-teal` diretamente (ex.: `index.html:390,400,409,446,459,466`). | CSS morto por rename; sem risco funcional. |
| Baixo | `style.css:238,244,250,256,262` (`.header-soft-blue/dark/green/orange/red`) | — | Variante alternativa de skin para cabeçalho de modal, nunca aplicada — os cabeçalhos de modal usam classes Bootstrap diretas (`bg-primary text-white`, `bg-dark text-white`, `bg-success text-white`, `bg-danger text-white`, `bg-warning-subtle`, conforme visto em praticamente todos os modais de `index.html`). | CSS morto, provável protótipo de skin que não foi adotado. |
| Baixo | `style.css:317,329,335,973,982,1005,1014,1024` (`.search-container-wrapper`, `.search-icon-wrapper`, `.search-input-clean`, `.search-input-hero`, `.search-pill-group`, `.search-pill-group-hero`) | — | Outra família de busca sem uso — os campos de busca reais usam `.proc-search-wrap`/`.proc-search-icon` (ex.: `index.html:599,604,1867,1871,1962`). | CSS morto por rename; ~6 regras (algumas com `:focus-within` e overrides `body.theme-dark`) nunca aplicadas. |
| Baixo | `style.css:1647` (`.btn-novo-orcamento`), `1619` (`.config-card-footer`), `179` (`.h-150`), `131` (`.icon-chat`), `680` (`.bg-indigo`) | — | Classes isoladas sem uso: botão "Novo Orçamento" usa classes Bootstrap genéricas no HTML atual, não essa classe dedicada; `.icon-chat` é órfã mesmo estando ao lado de `.icon-cloud`/`.icon-trash` (essas duas, em uso); `.bg-indigo` seria uma variante de cor de tile de módulo (`home-action-icon.bg-indigo`) não usada em nenhum dos tiles atuais (que usam green/blue/slate/purple/teal/orange). | Peso morto pontual, sem risco funcional. |
| Baixo (informativo) | `style.css:3639-3712` (`body.theme-dark .dropdown-item`, `.btn-outline-dark`, `.btn-white`) | — | Diferente dos demais: `dropdown-item`, `btn-outline-dark` e `btn-white` são classes **do próprio Bootstrap**, mas nenhum elemento do HTML atual usa essas 3 classes (nenhum dropdown Bootstrap nem botão com essas variantes existe hoje em `index.html`/`cronograma.html`). O override de tema escuro para elas é, portanto, código morto **hoje** — mas diferente das demais famílias acima, não é exatamente "renomeação": é preparação para um componente Bootstrap que não chegou a ser usado. | Nenhum impacto atual; só vale a pena remover se confirmado que não há plano de usar dropdown/esses botões futuramente. |
| Baixo | `style.css:60` (`.badge-status-revisao`), `2335` (`.badge-status-light-green`) | — | O gerador de classes de badge de status (`getGpStatusClass()`, `main.js:2209-2224`, e sua cópia em `main.js:3760-3772`) produz apenas as 10 classes listadas em `GP_STATUS_BADGE_CLASSES` (linha 2209-2211) — `badge-status-revisao` e `badge-status-light-green` não fazem parte desse conjunto e não são geradas em nenhum outro ponto do código. | CSS morto — variantes de badge que talvez tenham existido para um status já removido/renomeado. |
| — (não é órfão — falso positivo, atenção) | `style.css:3276-3287` (`.swal2-html-container`, `.swal2-html-container table/th`) | — | Não conta como órfão: `swal2-html-container` é uma classe **gerada dinamicamente pela própria biblioteca SweetAlert2** dentro do DOM que ela injeta em tempo de execução (`Swal.fire({html: ...})`), nunca aparecendo como string literal em `index.html`/`.js` — exatamente o caso de "classe gerada externamente" mencionado como cuidado a tomar. Está corretamente em uso sempre que um `Swal.fire` com HTML/tabela é exibido. | Nenhuma ação — mantido fora da lista de achados por ser confirmadamente em uso. |

**Resumo do bloco:** `style.css` tem **39 classes órfãs confirmadas** (de 338 analisadas, ~11,5%) e **zero ids órfãos**. O padrão dominante não é "CSS nunca usado", mas **resíduo de pelo menos 3 redesigns sucessivos** que renomearam classes sem remover a versão anterior: histórico de comentários (`history-*` → `history-card-*`), indicadores do painel Admin (`admin-stat-*` → `proc-kpi-*`), e campo de busca (`search-*` → `proc-search-*`) — em cada um desses três casos, a classe antiga e a nova coexistem em `style.css`, mas só a nova está em uso real. Um caso (`swal2-html-container`) foi investigado e descartado por ser gerado dinamicamente por biblioteca externa, não por template JS interno — nenhuma classe da própria aplicação (construída via template string em `main.js`/`admin.js`/etc.) escapou da varredura por esse motivo.

### BLOCO 6 — Compatibilidade

Não auditável apenas por leitura de código — depende de teste manual em Chrome, Edge, Firefox e viewport mobile. Ver checklist de testes ao final.

---

## 4. PLANO DE CORREÇÃO

**Status: itens 3, 4, 5, 8 e 9 aplicados nesta rodada (aprovação do usuário em 2026-07-26). Itens 1 e 2 seguem fora de escopo. Itens 6 e 7 foram avaliados e propositalmente NÃO aplicados — ver justificativa em cada um.**

1. **[Crítico, fora de escopo] Chave da Evolution API exposta** (`config.js:7`, uso em `whatsapp.js`) — a correção real (rotear todas as chamadas pelo `evo-proxy/` já existente, removendo `EVO_API_KEY` do front-end) **está fora do escopo desta revisão** (é mudança de arquitetura/backend). Não aplicado.
2. **[Crítico, fora de escopo] Bypass de admin via sessionStorage** (`admin.js`) — a correção definitiva exige validação server-side (RLS/Supabase policies), fora do escopo de arquivos elegíveis. Não aplicado.
3. **[Aplicado ✅] XSS armazenado via nome de usuário** — duas camadas aplicadas: (a) `signUpRequest()` em `main.js` agora sanitiza nome/sobrenome (allowlist de letras/espaço/hífen/apóstrofo) e matrícula (alfanumérico), rejeitando o cadastro se o resultado ficar vazio; (b) todos os pontos de exibição de nome/comentário/descrição identificados (`main.js`: modal e listagem em acordeão de comentários de Orçamentos e Composições, painel de Atividades da Home e aba dedicada, `nome_obra`/`descricao`/`subcategoria`/`autorV1`) agora passam por `escapeHTML()` antes do `innerHTML`. Links de anexo (`c.arquivo`) gerados nesses mesmos pontos também ganharam `rel="noopener noreferrer"`. `index.html`: `maxlength` adicionado em `reg-nome`/`reg-sobrenome` (60) e `reg-matricula` (20).
4. **[Aplicado ✅] Bypass de controle de acesso via `.or()` do PostgREST** (`main.js`, funções `carregarAtividades`/`carregarAtividadesResumoHome`) — o nome do usuário agora tem vírgula e parênteses removidos (`safeUserName`) antes de entrar na cláusula `.or()`, eliminando a possibilidade de forjar uma condição extra no filtro por perfil `fiscal`. Aplicado independente de RLS, por decisão do usuário.
5. **[Aplicado ✅] `rel="noopener noreferrer"`** adicionado nos 3 links `target="_blank"` de `index.html` (painel Evolution API ×2, sindusconce).
6. **[Avaliado e NÃO aplicado] `loading="lazy"` na única `<img>`** — a imagem (`brasao.png`, `index.html:47`) é o logo da tela de login/landing, visível imediatamente no primeiro carregamento para qualquer visitante não autenticado (above-the-fold). Aplicar `loading="lazy"` a uma imagem visível de início é contraindicado (atrasaria o próprio carregamento que o usuário vê primeiro) e é sinalizado como anti-padrão por ferramentas como Lighthouse. Mantido sem o atributo.
7. **[Avaliado e NÃO aplicado] `defer` no script do Supabase** (`index.html:22`) — confirmado que **não é seguro**: os 11 `<script src>` locais no fim do `<body>` (`config.js` … `main.js`, linhas 4392-4406) não têm `defer`/`async`, logo executam de forma síncrona assim que o parser HTML os alcança — **antes** de qualquer script com `defer` do `<head>` rodar (scripts deferidos só executam depois que o parsing do documento termina). `database.js` chama `sbClient = supabase.createClient(...)` nesse trecho síncrono, dependendo de `window.supabase` já existir. Adicionar `defer` ao CDN do Supabase adiaria sua execução para depois de `database.js`, quebrando `initSupabaseClient()` (ficaria `sbClient = null` permanentemente). Mantido sem `defer`.
8. **[Aplicado ✅] Remoção de CSS órfão em `style.css`** — as 39 classes/seletores confirmados sem nenhuma referência foram removidos: família `history-*` antiga (6 seletores), `admin-stat-*`/`admin-accordion-header`/`admin-section-title` (9), `body:not(.is-admin) .admin-tab` (1), `icon-bg-*` (5), `header-soft-*` (5), família `search-*`/`search-*-hero` (8, incluindo overrides de tema escuro), `btn-novo-orcamento`, `config-card-footer`, `.h-150`, `.icon-chat`, `.bg-indigo` (isolados, 5), `badge-status-revisao`, `badge-status-light-green` (2), `dropdown-item`/`btn-outline-dark`/`btn-white` (3, removidos das regras de tema escuro, preservando `.btn-light`/`.btn-outline-secondary` que continuam em uso). Balanceamento de chaves `{}` do arquivo verificado após a remoção (542/542).
9. **[Aplicado ✅] Padronizar validação de formulários** — coberto pelo item 3 (sanitização de entrada + `maxlength`).

**Validação técnica realizada:** `node --check main.js` sem erros de sintaxe; `style.css` com chaves balanceadas; `index.html` servido localmente (HTTP 200). **Não foi possível validar visualmente no navegador** (login real, geração de PDF/Excel, layout) nesta sessão — recomenda-se rodar o checklist da seção 6 antes de mergear.

## 5. REQUER CONFIRMAÇÃO (atualizado após o bloco de scripts inline/formulários/CSS)

- Policies de Row Level Security no painel do Supabase — preciso saber se existem e são restritivas, para dimensionar corretamente a severidade real do bypass de admin, da exposição da anon key **e agora também do bypass via `.or()` (item 4 do plano)**.
- ~~Rotina de cadastro de usuário ("Primeiro acesso") — se já existe alguma sanitização de nome~~ → **Confirmado nesta rodada: não existe nenhuma sanitização.** Ver item 3 do plano de correção para as opções de correção propostas.
- Reorganização dos 11 `<script src>` locais (`index.html:4392-4406`) para o fim do `<body>` de forma mais limpa — não vou mexer sem sua aprovação, já que hoje "funciona".
- Se a chave da Evolution API e o bypass de admin devem ser tratados nesta revisão (mesmo estando a correção completa fora do escopo estrito de `index.html`) ou registrados apenas como achados para outro time/tarefa.
- Todas as 186 ocorrências de `innerHTML` — vou precisar revisar uma a uma antes de trocar qualquer uma por `textContent`, para não quebrar formatação HTML intencional (badges, ícones, links dentro de mensagens etc.).
- Remoção das 39 classes CSS órfãs listadas no bloco de CSS órfão — qual escopo aprovar: remover todas, só as "isoladas" de baixo risco, ou nenhuma nesta rodada.

## 6. CHECKLIST DE TESTES MANUAIS PÓS-ALTERAÇÃO

- [ ] Login (usuário comum e admin) em `index.html` e em `cronograma.html`
- [ ] Cadastro de novo usuário ("Primeiro acesso") e aprovação por admin
- [ ] Navegação `index.html` ↔ `cronograma.html` (incluindo querystring `?pane=pane-reuniao`)
- [ ] Envio de mensagem WhatsApp (se a correção da chave for aplicada)
- [ ] Geração de relatórios PDF/Excel/Word (jsPDF, html2pdf, xlsx, exceljs, docx, pdf-lib)
- [ ] Gráficos Plotly (curva ABC, dashboard)
- [ ] Abertura dos links corrigidos com `target="_blank"` (painel Evolution API, sindusconce) em nova aba
- [ ] Responsividade em viewport mobile (landing/login overlay, tabelas, modais)
- [ ] Chrome, Edge e Firefox — visual idêntico ao estado atual
- [ ] Console do navegador sem novos erros/warnings após cada alteração
- [ ] Regressão geral do fluxo principal (login → painel → abrir processo → editar → salvar)
- [ ] Comentário com formatação rica (negrito/itálico/tamanho) no item da Curva ABC — salvar, reabrir, gerar relatório de comentários (confirma que o DOMPurify não descartou a formatação)
- [ ] Painel Admin: aprovar/recusar usuário pendente, aprovar a partir de notificação, editar e excluir usuário ativo (confirma que os `data-*` no lugar de `onclick` com string continuam funcionando)
- [ ] Disparo manual de WhatsApp (seleção de destinatário no `<select>`) e verificação de status da instância (timeout de 15s não deve interromper operações normais)
- [ ] Aba Orçamentos/Composições: comentários (modal e listagem em acordeão), histórico e anexos
- [ ] Aba Reunião/Processos: busca, ordenação por coluna (seta no cabeçalho), abrir modal de Meta e histórico de metas

## 7. SEGUNDA RODADA DE CORREÇÕES (2026-07-26) — aplicada após aprovação explícita

Escopo aprovado pelo usuário: "Prossiga" para atacar os achados restantes da auditoria (além dos já aplicados na seção 4), com duas exceções decididas explicitamente:
- **Backdoor `promoteAdmin99030487`** (`admin.js`/`main.js`) — **mantido intencionalmente**, por ser o mecanismo de auto-promoção do próprio usuário administrador (confirmado via `raw_line.txt`, que referencia `C:\Users\99030487\...`). Não foi alterado.
- **Comentário rich-text da Curva ABC** — em vez de `escapeHTML()` (que quebraria a formatação), foi adicionado **DOMPurify** (CDN, `index.html`) com uma allowlist de tags de formatação (`b/i/u/font/span/div/p/br/ul/ol/li`, atributo `style`), via novo helper `sanitizeRichHTML()` em `utils.js`.

### Segurança (XSS / injeção)

| Arquivo | O que foi corrigido |
|---|---|
| `relatorio.js` | `escapeHTML()` em `descricao`, `item.origem`, `item.descricao`, `f.nome` (fornecedor) e `r.descInsumo`/`r.codInsumo` no relatório de composição (HTML impresso). |
| `whatsapp.js` | `escapeHTML()` em `data-name`, `data-email`, valor e texto da `<option>` de destinatário (disparo manual). |
| `admin.js` | Reescritos os 3 blocos de renderização da tabela de usuários (pendentes, notificações, ativos): todo texto exibido passa por `escapeHTML()`, e os `onclick="fn('${valor}')"` com strings interpoladas (vulneráveis a quebra de contexto JS via aspas) foram trocados por atributos `data-*` lidos via `this.dataset.*` — elimina a injeção de JS arbitrário no navegador do admin, não apenas o XSS de exibição. |
| `curva_abc.js` / `curva_abc_processo.js` | Campo `comentario` (rich-text) agora passa por `sanitizeRichHTML()` (DOMPurify) tanto ao salvar (`cvSalvarComentarioItem`) quanto ao exibir (editor e os 2 relatórios de comentários), mantendo negrito/itálico/tamanho/alinhamento. |
| `curva_abc_processo.js` | Corrigido bug de sintaxe: `numero` (string tipo `"01"`) interpolado sem aspas dentro de um `onclick` — virava um literal numérico inválido; agora vai entre aspas. |
| `main.js` | Filtro `.or()` do PostgREST (`carregarAtividades`/`carregarAtividadesResumoHome`) agora remove vírgula/parênteses do nome do usuário antes de interpolar — fecha o bypass de controle de acesso por perfil `fiscal` reportado na seção 4 (mesma raiz, chamadas adicionais confirmadas nesta rodada). |

### Código morto / duplicado

| Arquivo | O que foi corrigido |
|---|---|
| `main.js` | Removidas as declarações **mortas** de `abrirModalAtender`/`abrirModalRecusar` (a primeira versão, nunca executada por causa de hoisting) e da versão solta de `deletarComposicao` (mantida a versão que reusa `deletarRegistroGenerico`, padronizando cursor de carregamento). Removido bloco de comentário de seção duplicado. |
| `dashboard.js` | Removida a **IIFE inteira duplicada** (linhas 1–261 do arquivo original, ~38%) — todas as funções que definia (`getSelectedValues`, `fillSelect`, `populateFinanceiroFilters` etc.) já eram redefinidas pela segunda IIFE, que é a única que executava de fato. Também removidas, da IIFE sobrevivente, as funções `groupCount`/`groupAvg`/`topN`/`quantile`/`renderHBar` — confirmado por grep em todo o repositório que nunca são chamadas (resíduo de uma versão anterior do dashboard). Arquivo caiu de 690 para ~410 linhas. |
| `utils.js` | Removidas `quantile`/`topN` (idêntico caso de código morto nunca chamado). |
| `database.js` | Removido `invokeFunction` (exposto em `window`, sem nenhuma chamada no repositório). |

### Robustez

| Arquivo | O que foi corrigido |
|---|---|
| `database.js` | `initSupabaseClient()` agora tem `try/catch` também na chamada via `window.addEventListener('load', ...)` (antes só a chamada síncrona tinha). Adicionado um aviso (`alert` + `console.error`) se, 4s após o `load`, o cliente Supabase ainda não tiver sido criado — cobre o caso de CDN bloqueado (ad-blocker/rede), que antes travava a tela sem nenhuma explicação. |
| `whatsapp.js` | Novo helper `fetchComTimeout()` (15s, via `AbortController`) usado nas 6 chamadas `fetch` à Evolution API — antes uma API travada deixava o spinner "Consultando..." pendurado indefinidamente. `lastNotificationCache` (Map de deduplicação) agora poda entradas expiradas a cada novo registro, em vez de crescer sem limite durante a sessão. |

### Qualidade de dados / desempenho

| Arquivo | O que foi corrigido |
|---|---|
| `main.js` | Corrigida a corrupção de acentuação em rótulos exibidos na tela e em PDFs oficiais: "CÓDIGO", "VERSÃO", "SUPERINTENDÊNCIA DE OBRAS PÚBLICAS", "PREÇO TOTAL DA COMPOSIÇÃO" (nos 2 relatórios HTML e no PDF via jsPDF). **Não alterado**: as strings usadas como chave de classificação/ordenação (`'SERVIO'`, `'MAO DE OBRA'` em `ordemGrupos`/`grupoDisplay`, compartilhadas entre `main.js` e `relatorio.js`) — corrigir a acentuação aí exigiria sincronizar 3 pontos diferentes (e `relatorio.js` já usa uma grafia *ainda mais* truncada, `"MO DE OBRA"`, que hoje nem bate com `main.js`) e mudaria o comportamento de ordenação das seções do relatório; fica como achado separado, não corrigido nesta rodada. |
| `main.js` | Removidos os `console.log('[DEBUG]...')` que imprimiam `sessionStorage` (usuário, role, e-mail, nome) no console — no `DOMContentLoaded` principal e no fluxo de `signInWithEmail`/IIFE de sessão. (Já eram no-op em produção via `IS_PROD=true`, linha ~410 — esta é uma camada extra de proteção caso `IS_PROD` seja revertido para depuração sem revisar os logs antes.) |
| `main.js` | `atualizarTabelaSuite` e `varrerRiscoDiligenciaSegundoPlano` faziam um `document.querySelector('tr[data-numero=...]')` por processo, dentro de um `.map(async...)` — O(processos × linhas da tabela), repetido a cada refresh e a cada 5 min para todos os "APROVADO". Agora constroem um `Map` numero→`<tr>` com uma única varredura do DOM antes do loop. |
| `main.js` | `updateReuniao()` só reescreve o `<thead>` da tabela quando o HTML do cabeçalho muda de fato (ex.: seta de ordenação) — antes reconstruía via `innerHTML` em toda chamada (filtro, busca, refresh), mesmo sem nenhuma mudança real. |
| `main.js` | **Não alterado** (avaliado e descartado por baixo risco/benefício): `window.abrirModalMeta` continua sendo redefinida a cada chamada de `updateReuniao()` — é uma função grande (~200 linhas) aninhada dentro dela; extrair com segurança exigiria confirmar que nenhuma referência interna depende de variáveis locais de `updateReuniao()`, e o custo real de recriar uma closure em JS é desprezível (V8 não aloca de forma cara). Risco de extração > benefício de performance nesse caso. |

### Validação técnica desta rodada

`node --check` sem erros em todos os 10 arquivos `.js` alterados (`main.js`, `admin.js`, `relatorio.js`, `whatsapp.js`, `curva_abc.js`, `curva_abc_processo.js`, `utils.js`, `database.js`, `dashboard.js`, `app.js`); `style.css` com chaves balanceadas; varredura por caracteres Unicode suspeitos (combinação/acentuação corrompida) em todos os arquivos tocados — nenhuma introduzida por esta rodada. **Segue sem teste em navegador real** (ver checklist da seção 6, itens novos ao final).

## 8. TERCEIRA RODADA — Desempenho da coluna Suíte (2026-07-27)

Origem: usuário relatou lentidão ao "processar" na aba Reunião/Processos após rodar os primeiros testes manuais do roteiro da seção 6. Diagnóstico apontou para a integração com a Edge Function `consultar-suite` (coluna "Suíte", badge "Consultando..."), não para o filtro/renderização da tabela em si.

**Causa raiz:** `atualizarTabelaSuite()` (`main.js`) e `varrerRiscoDiligenciaSegundoPlano()` (`main.js`, varredura de 5 em 5 min para alertar pré-diligência) escalonavam cada consulta não cacheada com um atraso de `índice × 150ms` antes de disparar o `fetch`. Com N processos não cacheados na tela, o último só começava a consultar depois de `N × 150ms` — dezenas de segundos com muitos processos — e o cache (`window.suiteCache`) só existia em memória, sendo descartado a cada F5, então o atraso se repetia em todo recarregamento de página dentro da janela de 5 min de frescor.

**Corrigido (aprovado pelo usuário, opção "as duas"):**
- Novo helper `executarComPool(items, limite, tarefa)` (`main.js`, antes de `varrerRiscoDiligenciaSegundoPlano`) — worker pool com no máximo 6 chamadas simultâneas à `consultar-suite`, substituindo o escalonamento sequencial nas duas funções. Tempo total deixa de crescer linearmente com o número de processos.
- `window.suiteCache` agora é hidratado de `sessionStorage` (`sop_suite_cache`) na primeira carga do script e persistido (`persistSuiteCache()`) depois de cada rodada de consultas — sobrevive a F5 dentro da mesma aba/sessão, evitando reconsultar tudo do zero a cada recarregamento. A checagem de frescor de 5 min nos pontos de uso não mudou.

**Validação técnica:** `node --check main.js` sem erros. **Ainda não testado no navegador** — validar no roteiro de teste (ver Artifact "Roteiro de Teste — GECOPE"): abrir a aba Reunião/Processos com muitos processos visíveis, cronometrar o tempo até todos os badges da coluna Suíte saírem de "Consultando..."; dar F5 dentro de 5 min e confirmar que a segunda carga é sensivelmente mais rápida (usa o cache de sessionStorage); confirmar que os alertas de pré-diligência (aba Aprovados) continuam aparecendo corretamente.

## 9. QUARTA RODADA — Revisão de bugs funcionais (AGENTE 1, 2026-08-13)

**Escopo:** revisão do zero, focada exclusivamente em bugs funcionais (lógica incorreta que causa comportamento errado) — não vulnerabilidades, não código morto, não performance pura. Cobriu o sistema inteiro, com atenção especial ao que mudou desde a última auditoria (2026-07-26/27): módulo Financeiro, SUITE/automação de status, Curva ABC, seletor de Tabelas, e os arquivos novos adicionados em agosto/2026 e nunca auditados antes (`consulta-contratos-proxy/index.js`, `portal_transparencia.html`, `gecope_mapa_obras.html`). Itens 181-186 e a seção 7 da auditoria anterior foram reconferidos: os que lá constavam como corrigidos seguem corrigidos (cache `_composicoesCarregadas` agora é respeitado; `database.js` já sinaliza falha de init; duplicações de `abrirModalAtender`/`abrirModalRecusar`/`deletarComposicao` seguem removidas); o achado sobre a chave de ordenação `'SERVIO'/'MAO DE OBRA'` (item de acentuação, seção 7) foi reaberto porque escondia um bug adicional não identificado antes — ver primeiro item abaixo.

### Bugs corrigidos

| # | Arquivo:linha | Bug | Correção |
|---|---|---|---|
| 1 | `relatorio.js:87,435` | `ordemGrupos`/`ordem` usavam a chave `'MO DE OBRA'` (faltando "A", não é só acento) para ordenar as seções do relatório de composição (HTML e exportação DOCX), enquanto `normalizarItemParaPDF` (`main.js:8016-8047`, usado pelos dois mesmos pontos de `relatorio.js`) sempre gera `grupo: 'MAO DE OBRA'`. Como as strings nunca batiam, `indexOf`/lookup retornava -1/undefined e a seção "Mão de Obra" caía sempre por último (depois de "GERAL"), em vez de vir primeiro. | Corrigido `'MO DE OBRA'` → `'MAO DE OBRA'` nos dois pontos, alinhando com a chave real gerada em `main.js`. |
| 2 | `admin.js:94,281` | `colspan="6"` (linha de "Carregando...") e `colspan="5"` (linha de erro) na tabela de usuários ativos, que tem 7 colunas (`index.html:643-649`). Só a linha de "Nenhum usuário ativo" (colspan="7") estava certa. | Ambos corrigidos para `colspan="7"`. |
| 3 | `dashboard.js:199-228` (Financeiro) | Filtro de "Ano" excluía processos sem `anoAbertura` (`.filter(v => v)` antes de `fillSelect`) — diferente de status/fiscal/contratada/contratante, que já dependiam da normalização "Não informado" do próprio `fillSelect`. Resultado: processos sem data de abertura válida somem silenciosamente de todos os KPIs/gráficos do Financeiro, sem nenhuma opção no filtro que os representasse. | Removido o `.filter(v => v)`; `fillSelect` agora gera a opção "Não informado" para esses registros (mesmo padrão dos outros 4 filtros) e a comparação em `getFinanceiroData` (linha ~226) foi ajustada para usar o mesmo fallback `"Não informado"` ao comparar `d.anoAbertura`. |
| 4 | `dashboard.js` (card "Redução sobre a repercussão") + `index.html:1228` | O rótulo do card era fixo ("Redução sobre a repercussão") mas o valor exibido muda de métrica (Repercussão/Acréscimo/Supressão) conforme o `<select id="metric-diff">` — ao selecionar "Acréscimo", o card mostrava o valor de redução sobre acréscimo com o rótulo ainda dizendo "repercussão", incoerente com o número ao lado. | Adicionado `id="kpi-reducao-label"` ao rótulo (`index.html`); `renderKPIsFinanceiro` (`dashboard.js`) agora atualiza esse texto conforme a métrica selecionada ("Redução sobre a repercussão/o acréscimo/a supressão"). |
| 5 | `main.js:1422-1436` (`carregarDadosFinanceiro`) | Falha ao buscar `vw_processos_financeiro` (rede, RLS, view indisponível) era só logada no console — o painel Financeiro ficava com todos os KPIs zerados, indistinguível de "nenhum processo corresponde ao filtro". | Adicionado `alert()` com a mensagem de erro no `catch`, avisando o usuário explicitamente que os dados financeiros não carregaram (mesmo padrão de robustez já usado em `carregarOrcamentos`/`carregarComposicoes`, que exibem o erro inline). |
| 6 | `main.js:4278-4317` (`atualizarTabelaSuite`) vs `main.js:790-796` (`varrerRiscoDiligenciaSegundoPlano`) | `aplicarAlertaPreDiligencia(...)` só era chamada dentro de `if (d.suite_data_chegada)`, embora a própria função (linha 590-618) não use `suite_data_chegada` em nada — depende só de status/sigla. Resultado: processos "APROVADO" que tramitam em setor de risco mas ainda sem `suite_data_chegada` preenchido na tabela `processos` não recebiam o ícone/contagem de alerta na aba "Aprovados" quando a tabela era re-renderizada por `atualizarTabelaSuite` — mesmo a varredura de segundo plano (`varrerRiscoDiligenciaSegundoPlano`, que chama a mesma função SEM essa guarda) aplicando o alerta corretamente a cada 5 min, o próximo re-render da tabela apagava/ocultava esse estado até a próxima rodada da varredura. | Chamada de `aplicarAlertaPreDiligencia` movida para fora do `if (d.suite_data_chegada)` em `atualizarTabelaSuite`, alinhando com o comportamento (correto) de `varrerRiscoDiligenciaSegundoPlano`. O badge "Tramitado"/exibição de "X dias" continuam dependendo de `suite_data_chegada` (isso é uma exibição de tempo decorrido, que genuinamente precisa da data). |
| 7 | `dashboard.js` (`populateFinanceiroFilters`) + `main.js:1648-1653` | Toda recarga de `window.financeiroData` — inclusive a disparada por criar/editar/excluir **qualquer** processo em qualquer outra aba — chamava `clearFinanceiro()` dentro de `carregarDadosSupabase()`, que força todas as opções dos 5 filtros do Financeiro a "selecionado" e re-renderiza. Um usuário com o Financeiro filtrado (ex.: só um fiscal) perdia a seleção sempre que qualquer processo era criado/editado/excluído no sistema, mesmo em outra aba. | (a) Removida a chamada a `clearFinanceiro()` de `carregarDadosSupabase()` (o botão explícito "Limpar filtros" continua chamando `clearFinanceiro()` normalmente, `main.js:4360`); (b) `populateFinanceiroFilters()` agora captura a seleção parcial de cada filtro antes de reconstruir as opções e a reaplica depois (se pelo menos uma opção antiga ainda existir na lista nova), preservando o filtro do usuário entre recargas de dados. |
| 8 | `consulta-contratos-proxy/index.js:41-56` (`ensureContext`) | Se a inicialização do browser/contexto falhasse uma vez (timeout no primeiro `page.goto`, falta de recurso no `chromium.launch`), `primingPromise` ficava permanentemente apontando para essa promise rejeitada — toda requisição futura (`/consulta`, `/edital`) voltava a falhar com o mesmo erro antigo para sempre, exigindo reiniciar o processo Node. | Adicionado `.catch()` na IIFE de inicialização que reseta `primingPromise = null` e `context = null` antes de relançar o erro, permitindo que a próxima requisição tente inicializar de novo. |
| 9 | `consulta-contratos-proxy/index.js:128-172` (`extrairTabela`) | Regex de `<thead>`, `<tbody>` e `<tr>` exigia a tag **sem nenhum atributo** (`/<tr>/`), diferente de `extrairTabelaPorId` (usada para o Licitaweb), que já aceitava atributos (`/<tr[^>]*>/`). Qualquer `<tr class="odd">`/`<tbody class="...">` real (comum em tabelas com zebra-striping) fazia as linhas da tabela serem ignoradas silenciosamente — `Aditivos`/`Ajustes` do contrato voltavam vazios mesmo quando a tabela tinha dados. | Regex de `thead`/`tbody`/`tr` em `extrairTabela` passaram a aceitar atributos (`[^>]*`), igual a `extrairTabelaPorId`. |
| 10 | `consulta-contratos-proxy/index.js:117-139` (`extrairCampos`) | Regex exigia que o atributo `class` fosse **exatamente** `content-with-label`/`content-label`/`content-value`, sem nenhuma classe extra. Página real (Bootstrap) frequentemente combina a classe semântica com classes de layout (ex.: `class="content-with-label col-md-6"`) — nesse caso a regex não casava e o campo inteiro ficava de fora de `campos{}`, virando `'N/A'` no contrato retornado. | Regex reescrita para aceitar classes/atributos extras (`\bclass=["'][^"']*\bcontent-with-label\b[^"']*["'][^>]*>`), mesmo padrão para os 3 seletores. |
| 11 | `consulta-contratos-proxy/index.js:126-139` (`extrairCampos`) | O fallback "usar o `href` do link" só disparava quando o texto visível estava **vazio** (caso do ícone de "Íntegra do contrato"). O campo "Ver edital" (consumido por `portal_transparencia.html:355-356` como `btnEdital.href = c.Ver_Edital`) é um link **com texto visível** ("Ver edital") — nesse caso o valor extraído era a legenda do botão, não a URL, e o botão "Ver edital" no front-end ficava com um `href` inválido (a própria string "Ver edital"). | Adicionado um segundo caso: quando o valor inteiro do campo é só um único `<a href=...>...</a>` (nada mais ao redor), usa o `href` mesmo que haja texto visível — cobre tanto o ícone vazio quanto o link com legenda. |
| 12 | `gecope_mapa_obras.html:679` (`aggIds`) | KPI "Paralisadas" contava obras com `o.statusObra === 'Paralisada'` (comparação exata), enquanto o gráfico/legenda de status (`statusBucket()`, linha 567-573, usado na linha 934) classifica via `normTxt(...).includes('PARALIS')` (tolerante a acento/caixa/texto adicional). Qualquer variação real de grafia (`"PARALISADA"`, `"Paralisada - aguardando retomada"`, etc.) fazia o número do KPI divergir do que o gráfico mostrava para o mesmo status. | `aggIds` agora usa `statusBucket(o.statusObra) === 'stop'`, a mesma classificação usada no gráfico. |
| 13 | `gecope_mapa_obras.html:548-558` (`classifyComissao`) | Os testes de dígito (`t.includes('1')`, `'2'`, `'3'`) rodavam **antes** do teste de `'SUPLENTE'`. Um valor real como `"1º Suplente"` contém o dígito "1", então caía no branch de `'1º MEMBRO'` (rank 4, titular) em vez de `'SUPLENTE'` (rank 1) — um suplente era exibido/ranqueado como membro titular da comissão de fiscalização. | Teste de `'SUPLENTE'` movido para antes dos testes de dígito. |

### Avaliados e não corrigidos (com justificativa)

- **`dashboard.js` (`calcularReducaoAgregada`, `calcularIndiceRevisao`, coluna "% Revisão" do drilldown)** — quando `repercFiscal` (ou o denominador equivalente) é 0 mas há diferença real (GECOPE atribuiu valor onde a Fiscalização não tinha nenhum), o percentual exibido é `0%` em vez de "indefinido"/"variação total". Isso é uma divisão por zero tratada de forma **consistente** em todos os 3 pontos do arquivo (mesma convenção, não uma divergência entre implementações) — trocar por outro valor (ex.: 100%, "N/A") é uma decisão de regra de negócio/formato de relatório, não uma correção óbvia de bug, e `_diffPerc` alimenta também a exportação Excel. **Requer confirmação do usuário** antes de mudar o comportamento.
- **`main.js:7487` / `main.js:8124` (`ordemCategorias`/`ordemGrupos`) e `main.js:6879` vs `relatorio.js` — grafias sem acento (`SERVIO`, `MAO DE OBRA`) usadas como chave de ordenação/agrupamento** — mantidas por decisão explícita do usuário na Segunda Rodada (seção 7); só a divergência literal `MO DE OBRA` × `MAO DE OBRA` (bug #1 acima) foi corrigida nesta rodada, por ser uma quebra de correspondência completa (não só falta de acento).
- **`main.js` — `toggleGrupo()` (composição analítica)** — primeiro clique no botão de alternar grupo de um item recém-adicionado (grupo ainda `undefined`) não altera nada visível (cai em `indexOf === -1 → 0 → 'MATERIAL'`, que já costuma ser o padrão). Comportamento levemente contraintuitivo, mas de baixo impacto e correção arriscada sem confirmação de qual deveria ser o comportamento esperado.
- **`cronograma.html` — `setRelView()`/`btn-view-lista`/`btn-view-cal`** — função referencia 2 ids (`btn-view-lista`, `btn-view-cal`) que não existem em `cronograma.html`. Investigado e confirmado que `setRelView()` não é chamada de nenhum lugar do arquivo (nem `onclick`, nem listener) — é código morto, não um bug ativo. Deixado para o agente de código morto.
- **`main.js:7487` (`renderizarItensComposicao`/relatório SEINFRA) — `ordemCategorias.findIndex(cat => a.includes(cat))`** — usa `.includes()` contra strings sem acento (`'MAO DE OBRA'`, `'EQUIPAMENTO'`); se o texto real de `item.categoria`/`item.identificacao` vier **com** acento do banco (ex. "MÃO DE OBRA"), a correspondência falha e o grupo cai no fim da ordenação por `localeCompare`. Mesma classe de risco do item de acentuação já aceito como decisão do usuário — não alterado nesta rodada, listado para confirmação caso o usuário quera revisar todas as chaves de ordenação de uma vez.

### Achados fora do escopo deste agente (para os próximos)

- `admin.js:744-745` (`window.diagnosePendingFetch` atribuído duas vezes) e outros resíduos de comentário duplicado em `main.js` — código morto, não mexido.
- `whatsapp.js` sem IIFE (variáveis globais) — performance/organização, não mexido.
- Backdoor `promoteAdmin99030487` duplicado em `admin.js`/`main.js` — mantido por decisão explícita do usuário (ver seção 4, item 2, e seção 7); segue fora do escopo de bugs funcionais.
- Uso extensivo de `innerHTML` com dados de usuário em pontos ainda não cobertos por `escapeHTML` nos módulos novos (Financeiro, SUITE, mapa de obras, portal da transparência) — segurança/XSS, não avaliado por este agente (fora de escopo).

### Validação técnica desta rodada

`node --check` sem erros nos 5 arquivos `.js` alterados: `main.js`, `admin.js`, `relatorio.js`, `dashboard.js`, `consulta-contratos-proxy/index.js`. Os blocos `<script>` inline de `index.html` e `gecope_mapa_obras.html` (arquivos HTML alterados) foram extraídos e parseados via `vm.Script` do Node — sem erro de sintaxe. Nenhum id duplicado introduzido em `index.html`. **Não testado no navegador** (fora do alcance deste agente) — recomenda-se validar manualmente: aba Financeiro (filtro de ano "Não informado", troca de métrica no card "Redução", persistência do filtro após criar/editar processo), aba Aprovados (alerta de pré-diligência aparecendo para processos sem `suite_data_chegada`), relatório de composição (ordem das seções, "Mão de Obra" primeiro), painel Admin (linhas de carregamento/erro da tabela de usuários), `gecope_mapa_obras.html` (KPI "Paralisadas" batendo com o gráfico, comissão com suplentes numerados exibindo o rótulo certo), e o proxy de contratos (`/consulta` e `/edital`, especialmente o botão "Ver edital" no `portal_transparencia.html` apontando para uma URL válida).

## 10. QUINTA RODADA — Revisão de vulnerabilidades (AGENTE 2, 2026-08-13)

**Escopo:** revisão do zero, exclusivamente de segurança, cobrindo o sistema inteiro (`index.html`, `main.js`, `admin.js`, `whatsapp.js`, `relatorio.js`, `dashboard.js`, `curva_abc.js`, `curva_abc_processo.js`, `utils.js`, `database.js`, `app.js`, `style.css`, `cronograma.html`, os arquivos novos de agosto/2026 — `gecope_mapa_obras.html`, `portal_transparencia.html`, `consulta-contratos-proxy/index.js`, `relatorio-analise-documental.html` — `evo-proxy/`, `worker/`, `functions/approve-user/`, `supabase/functions/*`, `consulta_contratos/*.py`, `sql/*.sql`). Reconfirmado que nenhuma correção de XSS das rodadas anteriores (seção 7) foi revertida por commits recentes: `escapeHTML()` segue presente em `main.js` (comentários/atividades/`nome_obra`), `relatorio.js` (5 ocorrências), `admin.js` (18 ocorrências, `onclick` continua em `data-*`, sem string interpolada), e `sanitizeRichHTML()`/DOMPurify seguem em uso em `curva_abc.js`/`curva_abc_processo.js`.

### Categoria A — corrigido

| # | Arquivo:linha | Vulnerabilidade | Correção |
|---|---|---|---|
| 1 | `gecope_mapa_obras.html` (múltiplos pontos: `obraCard`, `openModal`/`fmtVal`/`fmtContratoExt`/`fmtCNPJ`/`fmtDateBR`/`fmtDateTimeBR`, `rankRows`, `fillFilters`) | XSS armazenado: página **pública, sem login**, renderiza via `innerHTML` dezenas de campos vindos direto da tabela `contratos_edificacao`/`comissao_fiscalizacao` (Supabase, live) — objeto, contratada, contratante, fiscal, status, município etc. — sem nenhum escape em todo o arquivo (não havia função de escape alguma). | Adicionado `escHtml()` e aplicado em todos os pontos de interpolação de texto vindo do banco (card de contrato, modal de detalhes, ranking por distrito/cidade, rótulos dos filtros). Campos estáticos de configuração (nomes de distrito/região/município, hardcoded no próprio arquivo) foram deixados como estavam — risco desprezível, não são dados de usuário. |
| 2 | `portal_transparencia.html:357,361,366,314` (`rUrl.href`, `btnIntegra.href`, `btnEdital.href`, coluna "Íntegra" da tabela) | `escapeHtml()` já existia mas só neutraliza caracteres HTML, não o **esquema da URL** — um `href="javascript:...​"` vindo do HTML raspado do site externo (Ceará Transparente/Licitaweb, via proxy) passaria intacto e executaria ao clicar. | Adicionado `safeUrl()` (só aceita `http:`/`https:`, senão retorna `#`) e aplicado nos 4 pontos que atribuem `href` a partir de dados do proxy. |
| 3 | `consulta-contratos-proxy/index.js:392` (`POST /consulta`, campo `id`) | `id` ia direto para o path da URL requisitada pelo servidor (`buscarContratoPorId`) sem validação nem `encodeURIComponent` — qualquer caractere (`/`, `?`, `#`...) alterava o path/querystring da requisição feita pelo browser headless dentro do domínio do Ceará Transparente. `sacc` já era protegido (`encodeURIComponent`); `/edital` já tinha allowlist de domínio (`s2gpr.sefaz.ce.gov.br`) — confirmado e mantido como estava. | Adicionada validação `/^\d+$/` em `id` antes do uso (o site real só aceita IDs numéricos); retorna 400 se inválido. |
| 4 | `cronograma.html` (~20 pontos: `htmlTarefa`, `abrirHistorico`, `renderRotinasGlobal`, calendário `chipTarefa`, relatório, cards de concluídas/analistas) | XSS armazenado: arquivo **não carrega `utils.js`** (sem `escapeHTML` disponível) e renderiza via `innerHTML`, sem nenhum escape, os campos livres `t.descricao`, `t.processo`, `t.obs`, `t.setorNome`, `an.nome`, `an.cargo`, `an.matricula`, `h.nota` (motivo de paralisação/alteração de prazo) e `r.desc`/`r.obs` (rotina) — todos preenchidos por qualquer usuário com acesso ao Cronograma. Um caso era **attribute-breakout** real: `tituloSync` (contém `t.processo`) era concatenado dentro de um atributo `title="..."` delimitado por aspas duplas sem escape — uma aspa dupla no processo injetava atributos/HTML arbitrários. | Adicionado `escHtml()` local e aplicado em todos os pontos acima. |
| 5 | `cronograma.html:3430` (`chipTarefa`, preview do calendário) | O código extraía texto puro de `t.obs` fazendo `tmp.innerHTML = t.obs` num `<div>` **descartável** e lendo `tmp.textContent` de volta — mas setar `innerHTML` com HTML não confiável já executa handlers/carrega recursos (`<img onerror>` etc.) mesmo em elemento fora do DOM, antes de o texto ser lido de volta. | Trocado por *strip* de tags via regex (`replace(/<[^>]*>/g,' ')`), que nunca interpreta o conteúdo como HTML; o texto extraído também passou a ser escapado antes de reexibido. |
| 6 | `cronograma.html` (`salvarTarefa`, e todos os pontos que exibem `t.obs`) | Campo "Observações" da tarefa é um `<div contenteditable>` com negrito/itálico/alinhamento (`document.execCommand`) — salvava e reexibia o HTML digitado **sem nenhuma sanitização**, diferente do padrão já usado para o comentário rich-text da Curva ABC. | Adicionado DOMPurify (CDN, mesma versão usada em `index.html`) + `sanitizeRich()` (mesma allowlist de `utils.js`/`sanitizeRichHTML`) aplicado ao salvar (`salvarTarefa`) e em todos os pontos que exibem `t.obs` (card da tarefa, tabela de relatório, carregar para edição). Preserva a formatação intencional (negrito/itálico/alinhamento). |
| 7 | `whatsapp.js:730` (`carregarLogsWhatsApp`, botão "Ver Detalhes do Erro") | **Attribute-breakout via `onclick`**: o HTML interpolava `erroFormatado`, `log.destinatario_nome`, `log.destinatario`, `log.erro_detalhe` (texto de erro da Evolution API, pode ecoar conteúdo de mensagem) direto dentro de um `onclick="Swal.fire({...})"` delimitado por aspas duplas — uma aspa dupla em qualquer desses campos injetava HTML/JS arbitrário na tela de logs, visível a admins. | `onclick` trocado para chamar `verDetalheErroWhatsApp('${log.id}')` (só o id, já era o padrão usado pelos botões vizinhos); nova função busca o log pelo id no Supabase e monta o `Swal.fire` inteiramente em JS (nunca passa pelo atributo HTML), com `escapeHTML()` em todos os campos interpolados no `html:` do SweetAlert2. |
| 8 | `whatsapp.js:717-724` (mesma tabela de logs) | `log.evento`, `log.destinatario_nome`, `log.destinatario`, `nupExibicao`/`objetoExibicao` (extraídos de `log.mensagem`, que ecoa `objeto`/NUP do processo — campo de texto livre editável no cadastro de processo) e `log.status` renderizados via `innerHTML` sem escape. | `escapeHTML()` aplicado em todos os campos interpolados na linha da tabela. |
| 9 | `main.js:4036,5361,7282,7466` | 4 links `target="_blank"` sem `rel="noopener noreferrer"` (SUITE, download de arquivo de orçamento, ORSE, SEINFRA) — tabnabbing. `obra.arquivo_url` (linha 5361) também estava sem `escapeHTML()` (campo de URL gravado no banco, interpolado direto no atributo `href`). | `rel="noopener noreferrer"` adicionado nos 4 pontos; `escapeHTML()` adicionado em `obra.arquivo_url`, no link ORSE (`gerarLinkOrse`) e no link SEINFRA (`gerarLinkSeinfra`) por consistência (defesa em profundidade, mesmo com baixa probabilidade de caractere de quebra nesses códigos importados). |

### Categoria A — verificado, sem achados novos

- `evo-proxy/web/index.js`, `evo-proxy/worker/worker.js`, `worker/worker.js`, `functions/approve-user/index.js`, `supabase/functions/consulta-ceara-transparente/index.ts`, `supabase/functions/sincronizar-suite/index.ts` — todos usam exclusivamente `process.env.*`/`Deno.env.get(...)` para credenciais (Supabase service role, chave da Evolution API, `SYNC_SECRET`); nenhum segredo hardcoded. `sincronizar-suite` exige header `x-sync-secret` correspondente a `Deno.env.get('SYNC_SECRET')`. `functions/approve-user/index.js` valida server-side (via `service_role`) que quem chama é `role='admin'` em `app_users` antes de inserir — é o único caminho de aprovação de usuário com checagem real no servidor (ver Categoria B, item 2, sobre os demais caminhos client-side em `admin.js`).
- `consulta_contratos/*.py` (`main.py`, `ceara_transparente.py`, `licitaweb.py`) — scripts de protótipo/CLI locais (`if __name__ == "__main__"`, entrada hardcoded no próprio arquivo), sem servidor HTTP exposto e sem segredos. Sem superfície de ataque de rede; nenhuma ação necessária.
- `relatorio-analise-documental.html` — mockup estático (sem `<script>`, sem dado dinâmico, conteúdo de exemplo hardcoded). Nada a corrigir.
- `sql/*.sql` — políticas RLS **existentes** confirmadas (grep por `POLICY`/`ROW LEVEL SECURITY`): `curva_abc_versoes`/`curva_abc_itens` (select/insert/update/delete), `alerta_retorno_comentarios` (select/insert), `checklist_documentacao_aditivo` (select/insert/delete), `cronograma_analistas`/`cronograma_tarefas`/`cronograma_rotinas` (política única `for all` via função `cronograma_is_staff()`, que checa `role in ('admin','gerente','fiscal')` em `app_users`). **Nenhuma política RLS local para `app_users`, `processos`, `contratos_edificacao` ou `comissao_fiscalizacao`** — ou essas tabelas têm RLS configurada só pelo painel do Supabase (fora do repo), ou não têm RLS restritiva. Não é possível confirmar sem acesso ao painel (mesma ressalva já registrada na seção 5).

### Categoria B — avaliado, NÃO implementado (decisão do usuário necessária)

1. **`config.js:6` `EVO_API_KEY` hardcoded exposta no front-end** — reconfirmado, achado antigo intacto. `whatsapp.js` faz **6 chamadas síncronas diretas** à Evolution API usando essa chave, todas esperando resposta imediata para atualizar a UI: `enviarMensagemIndividual()` (linha ~491, disparo de notificação — manual e automático), `reenviarMensagemWhatsApp(logId)` (linha ~790, reenvio de mensagem falhada), `consultarStatusWhatsApp(messageId, logId, destinatario)` (linha ~816, status de entrega de uma mensagem), `verificarStatusEvolution()` (linha ~936, conectado/desconectado da instância), `criarInstanciaEvolution()` (linha ~966, criação de instância/fluxo de QR code), `window.testarConexaoWhatsApp` (linha ~1001, botão "Testar Conexão" do painel admin). O `evo-proxy/` hoje só expõe `/enqueue` (fila assíncrona via `whatsapp_jobs` + worker) — migrar exigiria criar nos 3 endpoints correspondentes no proxy (envio, status de instância/QR, status de mensagem) e adaptar essas 6 funções para poll/aguardar resposta assíncrona. Não implementado.
2. **`admin.js` `SENHA_MESTRA` hardcoded + bypass de admin via `sessionStorage.setItem('is_admin_gecope','true')`** — reconfirmado. `admin.js` grava/edita `app_users` de dois jeitos: (a) via Edge Function `approve-user` (linha ~495, **com** checagem server-side de admin — ver acima), e (b) via chamadas diretas `sbClient.from('app_users').insert/update/delete(...)` em pelo menos 10 pontos (linhas 351, 354, 395, 441, 451, 469-471, 553, 606, 716 — incluindo o próprio `promoteAdmin99030487`, linha ~716, que faz `update({role:'admin'})`), que dependem **inteiramente** de RLS no Supabase para não serem exploráveis por qualquer usuário autenticado via console do navegador. Como nenhum `sql/*.sql` do repositório define política RLS para `app_users` (ver acima), não é possível confirmar se essas 10 chamadas estão protegidas no servidor ou não. Preparado (não aplicado) `sql/proposta_rls_app_users.sql` com uma sugestão de política, marcado explicitamente como PROPOSTA.
3. **Backdoor `promoteAdmin99030487`** (`admin.js`/`main.js`) — reavaliado e **mantido**, conforme decisão do usuário de 2026-07-26. Não foi encontrado hardening barato que não arrisque travar o próprio dono do sistema fora do admin (ex.: uma janela de tempo exigiria saber a data de "instalação", que não existe no código; um log adicional não muda o risco real, que depende só de RLS em `app_users`). Nenhuma alteração aplicada.
4. **Rotação/config de infraestrutura fora do repo** — chave da Evolution API (`sopgecope2026`) nunca rotacionada; policies RLS reais só confirmáveis no painel do Supabase; variáveis de ambiente de produção dos serviços (`evo-proxy`, `consulta-contratos-proxy`, `worker`, `supabase/functions`) não auditáveis por leitura de arquivo. Apenas documentado.

### Validação técnica desta rodada

`node --check` sem erros em `main.js`, `whatsapp.js`, `consulta-contratos-proxy/index.js` (únicos `.js` puros alterados). Os blocos `<script>` inline de `gecope_mapa_obras.html`, `portal_transparencia.html` e `cronograma.html` (2 blocos) foram extraídos e parseados via `vm.Script` do Node — sem erro de sintaxe em nenhum. **Não testado no navegador** — recomenda-se validar manualmente: Mapa de Obras (modal de contrato, ranking por distrito/cidade, filtros), Portal da Transparência (busca por SACC/ID, botões "Ver edital"/"Íntegra do contrato" abrindo URL correta), Cronograma (criar/editar tarefa com observação em negrito/itálico, motivo de paralisação/alteração de prazo, relatório por analista, calendário), painel de Logs do WhatsApp (botão "Ver Detalhes do Erro", reenvio), e os 4 links corrigidos em `main.js` (SUITE, baixar orçamento, ORSE, SEINFRA) abrindo em nova aba sem erro de console.

## 11. SEXTA RODADA — Limpeza de código morto (AGENTE 3, 2026-08-13)

**Escopo:** varredura dedicada de código morto/resíduo em todo o sistema (`index.html`, `main.js`, `admin.js`, `whatsapp.js`, `relatorio.js`, `dashboard.js`, `curva_abc.js`, `curva_abc_processo.js`, `utils.js`, `database.js`, `app.js`, `config.js`, `style.css`, `cronograma.html`, `gecope_mapa_obras.html`, `portal_transparencia.html`, `relatorio-analise-documental.html`, `consulta-contratos-proxy/index.js`, `evo-proxy/`, `worker/`, `functions/approve-user/`, `supabase/functions/*`). Metodologia: scripts Node ad-hoc para (a) achar `function nome(` duplicadas na mesma escopo (regressão da classe de bug hoisting já corrigida em rodadas anteriores), (b) contar, para cada `function nome(`/`window.nome =`/`const nome = (arrow)`, quantas vezes o identificador aparece em todo o repositório (HTML+JS) além da própria declaração, (c) extrair todos os seletores `.classe`/`#id` de `style.css` e do `<style>` inline de `cronograma.html` e cruzar contra `class=`, `classList`, `getElementById`, `querySelector*` e template strings em todos os arquivos. Todo achado de "zero referências" foi conferido manualmente antes de remover, para descartar falsos positivos (classes montadas via template string como `` `badge-${tp}` ``, e substrings de URLs dentro de CSS como `w3.org` sendo capturadas pelo extrator de seletores).

### Removido (confirmado morto por grep completo)

| Arquivo:linha | O que era | Motivo |
|---|---|---|
| `admin.js:744-745` | `window.diagnosePendingFetch = diagnosePendingFetch;` duplicada (2x seguidas, idêntica) | Resíduo de edição; a segunda atribuição é redundante. Removida 1 das 2 linhas. |
| `cronograma.html` (`setRelView(view)`) | Função que alternava lista/calendário do relatório, referenciando `#btn-view-lista`/`#btn-view-cal` — ids que não existem em nenhum lugar do arquivo | Já identificada como morta pelo AGENTE 1 (seção 9) antes das edições de XSS do AGENTE 2; reconferida após essas edições e confirmada ainda morta (0 chamadas em todo o arquivo). Removida. |
| `cronograma.html` (`htmlRotinaCompacto(r)`) | Função de renderização compacta de rotina, nunca chamada — a renderização real de rotinas usa `renderRotinasGlobal()` com marcação `.rotina-linha-*` | 0 referências fora da própria declaração. Removida. `.rotina-freq` (classe usada dentro dela) segue em uso por `renderRotinasGlobal`, não foi afetada. |
| `cronograma.html` CSS: `.rotina-item`, `.rotina-desc`, `.rotina-acoes` (+ referência em `body.theme-dark .rotina-item` e no `@media print`) | Família de classes de uma versão anterior da lista de rotinas, substituída por `.rotina-linha`/`.rotina-linha-*` (renomeação sem limpeza, mesmo padrão já visto em `style.css` na Segunda Rodada) | 0 referências em `class=`/JS. Removidas as 3 classes e as 3 menções em overrides de tema escuro/impressão. |
| `cronograma.html` CSS: `.card-analista-footer` | Bloco de rodapé de card de analista sem nenhum elemento usando a classe | 0 referências. Removida. |
| `cronograma.html` CSS: `.rel-view-toggle`, `.rel-view-btn` (+ override `body.theme-dark .rel-view-btn`) | CSS dos botões de alternância lista/calendário — os próprios botões (`#btn-view-lista`/`#btn-view-cal`) nunca existiram no HTML (mesmo achado do `setRelView` morto) | 0 referências. Removidas. |
| `cronograma.html` CSS: `.hist-wrap`, `.hist-toggle`, `.hist-arrow`, `.hist-corpo`, `.hist-linha`, `.hist-linha-esq`, `.hist-data`, `.hist-tipo` (+ variantes `.hist-tipo.*`), `.hist-nota` (+ 4 menções em overrides de tema escuro) | Família CSS de uma versão anterior do histórico de tarefa, substituída pela família `mh-*` (`mh-resumo-item`, `mh-ri-label`, `mh-lt-detalhe` etc.) usada pela função ativa `abrirHistorico()` | 0 referências em `class=`/JS (confirmado lendo o corpo atual de `abrirHistorico`, que usa exclusivamente `mh-*`). Removida a família inteira (~50 linhas de CSS) e as 4 referências em regras de tema escuro compostas (mantendo as demais classes dessas mesmas regras, que seguem em uso). |
| `gecope_mapa_obras.html`: `GCOL` (array), `gColor()`, `cityThr`, `computeCityScale()`, `cityColor()` | Sistema alternativo de coloração de município por escala de valor, nunca chamado — e **estruturalmente quebrado**: `cityColor()` referenciava `RAMP[st.metric]`, e `RAMP` não é declarada em nenhum lugar do arquivo (chamar a função geraria `ReferenceError`) | 0 referências de uso; `RAMP` confirmada inexistente via grep no arquivo inteiro — evidência forte de código abandonado de uma versão anterior do mapa (a coloração real usa `groupStyle()`/`groupHover()`, cores sólidas). Removidas as 5 declarações. |
| `gecope_mapa_obras.html`: `hx()`, `mix()` | Helpers de mistura de cor RGB, cujo único chamador (`cityColor`) foi removido acima | Após remover `cityColor`, `mix()` ficou com 0 referências (era chamada só por `cityColor`); `hx()` era chamada só por `mix()`. Removidas em cascata, na mesma edição, e reconfirmado com novo grep que nada mais os chama. |
| `main.js` (comentários antes de `promoteAdmin99030487`) | 4 cabeçalhos de comentário `/* ... */` empilhados sem código entre eles — 3 descreviam funções que não existem mais no arquivo ("criar administrador inicial" ×2, "DIAGNÓSTICO E CORREÇÃO DE USUÁRIOS"), e um tinha um `/*` aninhado indevidamente dentro de outro comentário | Resíduo de edições sucessivas, já sinalizado na Segunda Rodada (seção 7, "Blocos duplicados/órfãos") mas não totalmente limpo. Substituídos por um único cabeçalho correto ("PROMOÇÃO AUTOMÁTICA DE ADMIN"). A função `promoteAdmin99030487()` em si **não foi tocada** — é o backdoor mantido por decisão explícita do usuário (não é código morto, está em uso ativo via `DOMContentLoaded`). |

### Verificado e confirmado NÃO órfão (falsos positivos descartados)

- `style.css`: reconferidas as 39 classes já removidas na Segunda Rodada — nenhuma nova classe/id órfão encontrado nesta rodada (338 seletores de classe re-extraídos e cruzados contra `index.html` + `cronograma.html` + todos os `.js` da raiz). Único "achado" (`swal2-html-container`) já é falso positivo catalogado (classe gerada em runtime pelo SweetAlert2).
- `cronograma.html` CSS: `badge-inicio`, `badge-execucao`, `badge-fim` — pareciam órfãos numa varredura textual simples porque são montadas via template string (`` `badge-${tp}` ``, `main.js`-equivalente em `cronograma.html:~3396`, dentro da função de badges do calendário); confirmadas em uso real após checar o código-fonte da montagem. **Não removidas.**
- 97 variáveis/funções atribuídas via `window.X = ...` em todos os arquivos raiz `.js` — todas reconferidas por contagem de referências em todo o repositório (HTML incluído, cobrindo `onclick="..."`); nenhuma com zero referências externas.
- Duplicação de `function nome(` na mesma declaração (a classe de bug que afetou `abrirModalAtender`/`abrirModalRecusar`/`deletarComposicao` em rodadas anteriores) — varredura completa em todos os 10 `.js` da raiz: nenhuma duplicata restante.
- `worker/worker.js` e `evo-proxy/worker/worker.js` são **byte-a-byte idênticos** — não é código morto (ambos são pontos de entrada de deploy distintos), é observação de duplicação de arquivo entre dois serviços; não removido (fora do escopo de "código morto", risco de derrubar um deploy). Anotado para os próximos agentes avaliarem se é intencional.

### Encontrado mas NÃO removido — requer confirmação (funcionalidade construída, porém não conectada a nenhum botão/evento)

Estes três casos são diferentes dos itens acima: não são resíduo de redesign (função/CSS renomeado e esquecido), são **funcionalidades completas e corretas, mas sem nenhum gatilho de UI que as chame hoje**. Remover destruiria uma feature potencialmente só desconectada por engano; por isso ficaram fora do escopo desta limpeza, conforme instrução de ser conservador diante de dúvida:

1. **`cronograma.html` — `abrirCalPopup(dStr, evt)` / `fecharCalPopup()`** (e a div `#cal-popup`, classes `.cp-item`/`.cp-secao-titulo` etc.) — função completa (~45 linhas) que monta um popup com as tarefas de um dia do calendário (início/execução/prazo), mas nenhuma célula `.cal-dia` do calendário tem `onclick` chamando `abrirCalPopup`. `fecharCalPopup` só é referenciada pelo listener de clique-fora. Parece uma feature "quase pronta" (falta só o `onclick` na célula do dia) — mais próximo de bug de wiring incompleto do que de código morto puro.
2. **`whatsapp.js:13` — `notificarAtualizacaoTabelas()`** — função completa que confirma via SweetAlert2 e dispara notificação em massa via WhatsApp avisando que uma tabela de referência (SEINFRA/SINAPI/ORSE) foi atualizada, lendo `#busca-fonte`/`#busca-versao`/`#busca-ref`. Não há nenhum botão na aba "Tabelas" chamando essa função.
3. **`whatsapp.js:812` — `consultarStatusWhatsApp(messageId, logId, destinatario)`** — função completa que consulta o status de entrega (pendente/enviado/entregue/lido) de uma mensagem já disparada. Citada no relatório da Quinta Rodada (seção 10, Categoria B item 1) como uma das 6 chamadas diretas à Evolution API, mas confirmado agora que **nenhum onclick/handler a chama** em todo o repositório.

Recomendação: decidir, para cada uma das três, se (a) é para conectar a um botão (feature esquecida a meio caminho) ou (b) é seguro remover como морта — decisão de produto, não de auditoria de código.

### Validação técnica desta rodada

`node --check` sem erros em todos os 10 `.js` da raiz (`main.js`, `admin.js`, `whatsapp.js`, `relatorio.js`, `dashboard.js`, `curva_abc.js`, `curva_abc_processo.js`, `utils.js`, `database.js`, `app.js` — só `main.js` e `admin.js` foram efetivamente alterados nesta rodada, os demais foram conferidos e não precisaram de mudança). Blocos `<script>` inline de `cronograma.html` e `gecope_mapa_obras.html` (únicos HTML alterados) extraídos e parseados via `vm.Script` do Node, sem erro de sintaxe. Chaves `{}` do `<style>` inline de `cronograma.html` recontadas antes/depois (358/358 balanceadas). Nova varredura de órfãos rodada uma segunda vez após as remoções, para garantir que a remoção de uma função não deixou outra órfã em cascata (caso real em `gecope_mapa_obras.html`: remover `cityColor()` deixou `mix()` — sua única chamadora — órfã, e remover `mix()` deixou `hx()` — chamada só por `mix()` — órfã também; as 3 foram removidas juntas após reconferência). **Não testado no navegador** — recomenda-se validar: Cronograma (aba Rotinas exibindo corretamente, histórico de tarefa `mh-*` abrindo normalmente, tema escuro sem quebra visual nos elementos que tiveram seletor removido de regras compostas), Mapa de Obras (ranking por distrito/cidade e cards de obra, já que `escHtml`/`rankRows`/`obraCard` foram tocados por este e pelo agente anterior).

**Redução líquida desta rodada** (medida por `wc -l` nos 4 arquivos efetivamente editados, início→fim da sessão): `main.js` 8411→8400 (-11), `admin.js` 755→754 (-1), `cronograma.html` 4052→3930 (-122), `gecope_mapa_obras.html` 1389→1370 (-19) — **153 linhas líquidas removidas no total**.

## 12. SÉTIMA RODADA — Otimização de performance (AGENTE 4, 2026-08-13)

**Escopo:** varredura dedicada de performance/celeridade em todo o sistema (`index.html`, `main.js`, `admin.js`, `whatsapp.js`, `relatorio.js`, `dashboard.js`, `curva_abc.js`, `curva_abc_processo.js`, `utils.js`, `database.js`, `app.js`, `style.css`, `cronograma.html`, `gecope_mapa_obras.html`, `portal_transparencia.html`, `consulta-contratos-proxy/index.js`, `evo-proxy/`, `worker/`), sem mexer em bugs funcionais, vulnerabilidades ou código morto puro (já cobertos pelos Agentes 1-3, seções 9-11). Reconfirmado que as otimizações das rodadas anteriores (Map em vez de `querySelector` em loop em `atualizarTabelaSuite`/`varrerRiscoDiligenciaSegundoPlano`, `thead` só reconstruído quando muda, worker pool de 6 chamadas simultâneas + cache em `sessionStorage` para SUITE) seguem intactas — nenhuma foi tocada ou revertida.

### Aplicado

| # | Arquivo:linha | Problema | Correção | Impacto esperado |
|---|---|---|---|---|
| 1 | `main.js` (era 289-295, listener `shown.bs.tab` em `tabComposicoes`) | Chamada **redundante** de `carregarComposicoes()` a cada vez que a aba Composições era reaberta, ignorando completamente a flag de cache `_composicoesCarregadas` que a função `showPane()` (linha ~3196) já respeita. Como `showPane()` é o único caminho que abre essa aba (o nav antigo `#dashboardTabs` está com `d-none` desde a substituição por tiles), esse listener disparava uma segunda consulta paginada completa ao Supabase **toda vez** que o usuário reabria a aba, além da que `showPane()` já fazia na primeira visita. Era exatamente o achado "Baixo" já catalogado na revisão original (bloco profundo de `main.js`, linhas 290-295 vs 3098-3109) e que tinha ficado pendente até esta rodada. | Listener removido; o carregamento com cache de `showPane()` é o único caminho agora. | Elimina uma consulta Supabase completa e desnecessária (com re-render de todo o acordeão de Composições) toda vez que o usuário navega de volta para essa aba dentro da mesma sessão. |
| 2 | `main.js` (era linha 3275, dentro do `DOMContentLoaded` principal) | `carregarOrcamentos()` era chamado **eager** no carregamento da página (antes de qualquer navegação do usuário) mas **sem** marcar `window._orcamentosCarregados = true` — resultado: na primeira vez que o usuário abria a aba Orçamentos, `showPane()` via a flag como `false` e disparava uma **segunda** consulta paginada idêntica (`orcamentos_biblioteca`, blocos de 1000 linhas via `while(hasMore)`), mesmo os dados já tendo acabado de ser buscados segundos antes. | Adicionado `window._orcamentosCarregados = true;` imediatamente antes da chamada eager, alinhando o comportamento com o que já existe para Composições. | Elimina uma consulta paginada completa e redundante (potencialmente múltiplos round-trips de 1000 linhas cada) na primeira vez que qualquer usuário abre a aba Orçamentos em cada sessão. |
| 3 | `main.js` (função `window.abrirModalMeta`, construção de `historicoHTML`, era linha 4146) | Dentro do `.map()` que renderiza cada linha do histórico de metas, `const pRow = (window.allData \|\| []).find(r => r.processo === processo)` era recalculado a cada iteração — mas `pRow` **não depende de `h`** (é sempre o mesmo processo para todas as linhas do histórico), então o `.find()` completo em `window.allData` rodava N vezes (N = nº de linhas de histórico) para produzir sempre o mesmo resultado. | `pRow` extraído para fora do `.map()` (calculado 1x, reaproveitado em cada iteração via `pRowHistorico`). | Baixo-médio: reduz de O(histórico × allData) para O(allData) o custo de abrir o modal "Definir Meta" de um processo com histórico longo; mais perceptível quanto maior `window.allData` (lista de processos) e mais entradas de histórico o processo tiver. |
| 4 | `curva_abc.js:942` (`$("cv-busca").addEventListener("input", desenhaTabela)`) | Busca da tabela da Curva ABC disparava `desenhaTabela()` (filtro completo do array de itens + reconstrução de `innerHTML` da tabela inteira) a **cada tecla digitada**, sem debounce — a ferramenta é usada justamente com planilhas grandes (o próprio propósito do módulo é analisar centenas/milhares de itens de composição). | Adicionado debounce local de 200ms (`cvDebounce`, definido no próprio arquivo — **não** reaproveita `window.debounce` de `main.js` porque `curva_abc.js` carrega **antes** de `main.js` na ordem de `<script src>` de `index.html`; nesse ponto do carregamento a função ainda não existe no escopo global, então uma cópia local isolada evita qualquer dependência de ordem de carregamento). | Elimina filtro+re-render completo a cada tecla; só roda 200ms após o usuário parar de digitar — sensível em planilhas grandes. |
| 5 | `dashboard.js` (era linha 555, busca do drilldown Financeiro `#fin-drilldown-search`) | Mesmo padrão do item 4: `renderDrilldownFinanceiro(getFinanceiroData())` — que filtra `window.financeiroData` inteiro e reconstrói a tabela de drilldown — rodava a cada tecla digitada, sem debounce. | Adicionado debounce local de 250ms (`dashDebounce`, definido no próprio arquivo pelo mesmo motivo de ordem de carregamento do item 4 — `dashboard.js` também carrega antes de `main.js`). | Elimina filtro+re-render completo a cada tecla na busca do drilldown Financeiro. |
| 6 | `gecope_mapa_obras.html:1235` (campo de busca `#fSearch`, página pública sem login) | `render()` (reestiliza toda a camada GeoJSON dos municípios via `layer.setStyle`, roda `applyInteractivity()`, `updateLabels()` — que inclui o algoritmo de anti-colisão de rótulos `declutter()` — e `renderPanel()`/`renderFoot()`) e `autoLocateSearch()` (pode disparar animação de câmera do Leaflet) rodavam a cada tecla digitada na busca, sem debounce. | Adicionado debounce local de 150ms; `st.f.q` continua sendo atualizado de forma síncrona a cada tecla (não afeta comportamento), só o `render()`/`autoLocateSearch()` (o trabalho pesado) foi adiado. | Reduz recomputação de estilo GeoJSON + declutter de rótulos + possível animação de câmera a cada tecla digitada; mais perceptível em dispositivos móveis/mais lentos, que são o público real dessa página pública. |

### Avaliado e NÃO alterado (achados sem impacto real confirmado ou fora de escopo)

- **`fachada.png` (2.3MB) não é referenciado em nenhum `.html`/`.js`/`.css`** — confirmado via grep em todo o repositório; só `fachada.jpg` (282KB) é usado (`index.html:16,183`, `style.css:991`). Como não é carregado pelo navegador em nenhum fluxo, **não tem impacto de performance real hoje** — é candidato a limpeza de asset morto (escopo do Agente 3, não deste agente). O mesmo vale para `brasao (1).png` (300KB, também sem nenhuma referência — só `brasao.png` é usado). Apenas anotado, nada removido.
- **273 ocorrências de `!important` em `style.css`** e 2 seletores universais (`* { ... }`, provavelmente reset de `box-sizing`) — não representam um problema de performance perceptível na escala deste projeto (CSS matching de `!important` não é caro; é uma questão de manutenção/especificidade, não de celeridade). Não alterado, conforme a própria diretriz da tarefa de que CSS caro não é prioridade alta neste stack.
- **`fetch()` sem `AbortController`/timeout em `main.js:7924,8339`** (download de composição analítica em JSON, ORSE/SEINFRA) e em `gecope_mapa_obras.html:596` (REST do Supabase) — são chamadas únicas disparadas por ação explícita do usuário (clique em botão), não polling em segundo plano, e já ficam dentro de fluxos com tratamento de erro. Diferente das 6 chamadas da Evolution API em `whatsapp.js` (já corrigidas na Segunda Rodada, seção 7, por serem propensas a travar a UI com "Consultando..." pendurado), o risco aqui é menor. **Requer confirmação** caso o usuário quera padronizar timeout em todas as chamadas de rede do sistema, não só nas identificadas como problemáticas.
- **Multiselect de filtros do Financeiro (`dashboard.js`, busca interna do dropdown, `searchInput.oninput`)** — reconfirmado da rodada de 2026-07-26 como impacto real baixo hoje (lista de opções tipicamente pequena, filtro em memória). Não alterado.
- **273 `<script>` locais sem `defer`/Supabase sem `defer` (`index.html:22`)** — reconfirmado como **não seguro** de alterar, pelo mesmo motivo já documentado na seção 4, item 7 (os 11 `<script src>` locais no fim do `<body>` executam de forma síncrona e dependem de `window.supabase` já existir; `defer` no CDN do Supabase quebraria `database.js`). Nenhuma tentativa de solução alternativa (ex.: mover a criação do cliente para dentro de um listener) foi aplicada nesta rodada — mudaria o timing de inicialização de um componente crítico (autenticação) e está fora do apetite de risco desta tarefa (correção "sem regressão de comportamento"). Mantido como estava.
- **Loop O(colunas×linhas) em `curva_abc.js:processarAoa`** (detecção de colunas numéricas a cada upload de planilha) — reconfirmado como baixo impacto: roda uma única vez por upload, não em resposta a interação contínua do usuário. Não alterado, conforme já avaliado na auditoria original.

### Validação técnica desta rodada

`node --check` sem erros em `main.js`, `curva_abc.js`, `dashboard.js` (os 3 arquivos `.js` alterados). O bloco `<script>` inline de `gecope_mapa_obras.html` (único HTML alterado) foi extraído e parseado via `vm.Script` do Node — sem erro de sintaxe. Nenhuma das otimizações de rodadas anteriores (Map em `atualizarTabelaSuite`/`varrerRiscoDiligenciaSegundoPlano`, `thead` condicional, worker pool + cache SUITE) foi tocada. **Não testado no navegador** — recomenda-se validar manualmente: aba Composições (dados aparecem normalmente ao entrar e ao reabrir a aba, sem tela em branco), aba Orçamentos (idem, primeira visita após login), modal "Definir Meta" de um processo com histórico de metas (valores de "Registro"/"Meta"/"Dias" continuam corretos, especialmente para entradas com `autor === 'Sistema'`), busca da Curva ABC (tabela filtra corretamente ~200ms após parar de digitar, sem parecer travada), busca do drilldown Financeiro (mesmo comportamento, ~250ms), e busca do Mapa de Obras (`gecope_mapa_obras.html`, filtro e "voo" da câmera até o município continuam funcionando ~150ms após parar de digitar).

## 13. OITAVA RODADA — Controle de funcionalidades (AGENTE 5, 2026-08-13)

**Escopo:** última linha de revisão desta auditoria, exclusivamente estática (leitura/grep/AST — sem navegador), com dois objetivos: (Fase 1) confirmar que as edições dos Agentes 1-4 (seções 9-12) não quebraram nenhum `onclick`/handler/id ao remover, renomear ou mover código; (Fase 2) auditoria geral de "todo botão/filtro/função tem handler real", cobrindo o sistema inteiro com atenção a Financeiro, Suíte, Curva ABC, Admin, WhatsApp, Cronograma, Mapa de Obras e Portal da Transparência.

**Metodologia:** scripts Node ad-hoc para (a) extrair todo `onXXX="..."` de todos os `.html` — inclusive HTML gerado dinamicamente via template string nos `.js` — e confirmar que cada função chamada está definida em algum `.js`/`.html` carregado pela mesma página; (b) extrair todo `id="..."` estático de `index.html`/`cronograma.html`/`gecope_mapa_obras.html` e cruzar contra todo `getElementById`/`querySelector('#...')`/`new bootstrap.Modal(...)` do JS correspondente, sinalizando referências sem id correspondente; (c) checar ids duplicados nos 4 HTML; (d) checar se algum id tem simultaneamente `onclick=` estático **e** um `addEventListener('click', ...)` em JS (risco de disparo duplo); (e) reconferir por grep de palavra inteira (`\b`) que as remoções de código morto da Sexta Rodada (seção 11: `GCOL`/`gColor`/`cityThr`/`computeCityScale`/`cityColor`/`hx`/`mix` em `gecope_mapa_obras.html`, `setRelView`/`htmlRotinaCompacto` em `cronograma.html`, a linha duplicada de `diagnosePendingFetch` em `admin.js`) seguem com zero referências reais (os "achados" de `gColor`/`hx`/`mix` eram falsos positivos de substring, ex. `groupBgColor`, `fgColor` — confirmado com `\b`); (f) reconferir os 6 pontos de `this.dataset.*` (a substituição onclick-interpolado→data-* da Quinta Rodada, todos em `admin.js`) batendo exatamente com os `data-*` presentes no mesmo botão (`data-email`, `data-role-select-id`→`dataset.roleSelectId`, `data-notif-id`→`dataset.notifId`, etc. — conversão kebab-case→camelCase confirmada correta nos 6 pontos); (g) reconferir a wiring da correção de XSS do `verDetalheErroWhatsApp` (`whatsapp.js`, seção 10 item 7) — função existe, busca o log pelo id e usa `escapeHTML()` internamente, como documentado.

### Verificado e confirmado OK (nenhuma regressão das rodadas 4-7)

- **145 chamadas `onXXX="..."` únicas** (estáticas + geradas via template string em todos os `.js`) resolvidas contra todas as funções definidas no sistema: **0 funções realmente ausentes**. 6 "faltantes" da primeira passada eram falsos positivos do extrator (métodos de objeto como `WhatsAppConfigManager.save()/.loadAll()/.prepareDirectMsg()/.sendDirectMsg()` capturados só pelo nome do método, e `window.print()`/`document.getElementById(...)` nativos capturados sem o prefixo) — todos confirmados corretos manualmente.
- **480 ids em `index.html`, 97 em `cronograma.html`, 34 em `gecope_mapa_obras.html`** cruzados contra todas as referências `getElementById`/`querySelector('#...')`/`bootstrap.Modal`: única divergência real de ids dinâmicos (`${item.key}_obs_wrap`/`_obs_toggle` em `main.js`) confirmada consistente com os 10 ids estáticos `chk_*_obs_wrap`/`chk_*_obs_toggle` de `index.html` e as chaves de `CHECKLIST_ADITIVO_ITENS` (`main.js:2142`) — todas batem 1:1. `aviso-datas-modal` (`cronograma.html`) é criado sob demanda pelo próprio código (`if (!avisoEl) { ... createElement ... }`) — não é referência quebrada.
- **0 ids duplicados** em `index.html`/`cronograma.html`/`gecope_mapa_obras.html`/`portal_transparencia.html`.
- **0 elementos com `onclick` estático e `addEventListener('click', ...)` simultâneos no mesmo id** — nenhum risco de disparo duplo introduzido pelas trocas onclick→data-*/addEventListener das rodadas anteriores.
- Remoções de código morto da Sexta Rodada (`GCOL`, `gColor`, `cityThr`, `computeCityScale`, `cityColor`, `hx`, `mix`, `setRelView`, `htmlRotinaCompacto`, linha duplicada de `diagnosePendingFetch`) — reconfirmadas com grep de palavra inteira: **zero referências residuais**, remoção segura.
- Filtros do Financeiro (`dashboard.js`) — os 5 filtros multiselect (status/fiscal/contratada/contratante/ano) despacham `change` sintético no `<select>` oculto, que é escutado por `fin.status/.fiscal/.contratada/.contratante/.ano.addEventListener("change", updateFinanceiro)` em `main.js:4370-4371`; `getFinanceiroData()` aplica os 5 filtros corretamente (inclusive "Não informado", fix do Agente 1 intacto). Sem regressão.
- Busca da Curva ABC (`curva_abc.js:953`), busca do drilldown Financeiro (`dashboard.js:566`) e busca do Mapa de Obras (`gecope_mapa_obras.html:1236`) — os 3 debounces da Sétima Rodada seguem como único listener de `input` em cada campo (sem listener duplicado/concorrente deixado para trás).
- Filtros dinâmicos do Mapa de Obras (`fillFilters()`/`#filtersHost`, `gecope_mapa_obras.html`) — delegação de evento (`change`/`click` no container pai) confirmada intacta e disparando `render()`/`autoLocateSearch()`.
- `admin.js`: os 6 pontos de `this.dataset.*` (aprovação/recusa de usuário, tabela de notificações pendentes) — todas as chaves `data-*` existem no mesmo elemento, conversão para `camelCase` correta, função-alvo (`aprovarUsuario`, `excluirUsuario`, `aprovarFromNotification`, `recusarNotification`, `abrirModalEdicaoUsuario`) definida e exposta em `window`.
- Formulários de `index.html` (26 `<form>`): em todos os casos o botão "Salvar" real é `type="button"` **fora** do `<form>` (no `modal-footer`) com `onclick="funçãoDeSalvar(...)"` — não depende de evento `submit`; os 2 únicos formulários que de fato usam `submit` (`landingLoginForm`, `landingRegisterForm`, mais `formLogin` do modal admin) têm `addEventListener('submit', ...)` confirmado em `main.js`/`admin.js`. Nenhum formulário tem dois listeners conflitantes no mesmo botão.
- `sanitizeRich()`/DOMPurify (`cronograma.html`, Quinta Rodada) e `escHtml()`/`safeUrl()` (`gecope_mapa_obras.html`/`portal_transparencia.html`, mesma rodada) seguem definidos e aplicados nos pontos documentados; CDN do DOMPurify carrega com `defer` mas `sanitizeRich()` só é chamado em resposta a interação do usuário (nunca no parse inicial), sem risco de timing.

### Encontrado — corrigido nesta rodada

| # | Arquivo:linha | O que estava errado | Correção | Causado por este ciclo de 4 agentes? |
|---|---|---|---|---|
| 1 | `main.js:4897` (`buildModuleSelector`) | Template string gerava `< div class= "modal fade" id = "modalModuleSelector" ...>` — espaço entre `<` e `div`. Pela regra de tokenização HTML5 (tag open state), `<` seguido de espaço **não** é reconhecido como abertura de tag; o parser emite o `<` como texto literal e o restante vira conteúdo solto, então nenhum elemento com `id="modalModuleSelector"` seria de fato criado no DOM. Toda chamada subsequente a `document.getElementById('modalModuleSelector')` (linhas 4914/4921/4929) retornaria `null`, e `.addEventListener` em `null` lançaria `TypeError`, quebrando a função inteira. | Corrigido para `<div class="modal fade" id="modalModuleSelector" tabindex="-1">`. | **Não.** `git diff HEAD -- main.js` confirma que a região de `buildModuleSelector` (linhas ~4886-4924) não foi tocada por nenhum dos 4 agentes anteriores — é um defeito pré-existente, presente antes desta revisão de 5 agentes começar. |

### Encontrado — NÃO corrigido, requer decisão do usuário (feature órfã, não é bug desta revisão)

- **`main.js` `buildModuleSelector()`/`openModuleSelector()` + `#btn-admin-login` (referenciado em `main.js:4937` e `admin.js:9`, via `.btn-admin-login`)** — todo o "seletor de módulos pós-login" (modal que deixa o usuário escolher quais abas/painéis ficam visíveis, persistido em `localStorage`) está **completamente inalcançável** hoje: o botão-gatilho `#btn-admin-login`/`.btn-admin-login` (um ícone de cadeado no cabeçalho, com CSS próprio ainda em `style.css:3069-3079`) não existe em nenhum lugar de `index.html`. Como o listener é anexado com `?.addEventListener(...)` (optional chaining), isso não gera erro no console — apenas silenciosamente não faz nada. Investigado e confirmado que a visibilidade real de abas hoje é 100% controlada por `applyRoleToUI()`/atributos `data-roles` (`main.js:4587+`), um sistema mais novo que claramente **substituiu** este seletor de módulos. Não é uma quebra desta revisão (confirmado via `git diff HEAD -- index.html`, que mudou só 1 linha, não relacionada) — é código órfão de um redesenho anterior que o Agente 3 (código morto) não capturou porque sua metodologia contava referências ao *identificador* (`btn-admin-login` tem 2 referências em JS) sem verificar se o *elemento HTML alvo* ainda existe. **Recomendação:** decisão de produto — (a) remover de vez `buildModuleSelector`, `openModuleSelector`, o listener de `btn-admin-login`, o `querySelectorAll('.btn-admin-login i')` de `admin.js:9` e o CSS órfão `.btn-admin-login` de `style.css`, já que `applyRoleToUI` cobre a mesma necessidade; ou (b) se a intenção era manter as duas formas de controle de visibilidade em paralelo, reintroduzir o botão no cabeçalho. Nenhuma ação tomada além do fix defensivo do template (item 1 acima), que elimina o `TypeError` latente sem alterar comportamento (a função continua inalcançável de qualquer forma).
- **`whatsapp.js` `notificarAtualizacaoTabelas()` e `consultarStatusWhatsApp()`, `cronograma.html` `abrirCalPopup()`/`fecharCalPopup()`** — reconfirmados exatamente como catalogados pelo Agente 3 (seção 11, "Encontrado mas NÃO removido"): funcionalidades completas, sem nenhum gatilho de UI. Nenhuma mudança de estado desde a Sexta Rodada.

### Observação registrada, sem ação (redundância inofensiva, não é bug)

- `escapeHTML()` está definida duas vezes de forma idêntica: em `utils.js:209` (`window.escapeHTML = ...`) e novamente como `function escapeHTML(str) {...}` solta em `main.js:1138`, com a mesma implementação byte-a-byte. Como `main.js` carrega por último, sua declaração apenas redefine a mesma função global sem mudar o comportamento — não é uma quebra, é duplicação cosmética já pré-existente. Deixado para uma eventual rodada de código morto (fora do escopo de "conexão quebrada" desta rodada).

### Validação técnica desta rodada

`node --check` sem erros nos 12 arquivos `.js`/`.js`-like do repositório (`main.js` — único alterado nesta rodada —, `admin.js`, `whatsapp.js`, `relatorio.js`, `dashboard.js`, `curva_abc.js`, `curva_abc_processo.js`, `utils.js`, `database.js`, `app.js`, `config.js`, `consulta-contratos-proxy/index.js`). Blocos `<script>` inline de `index.html` (2), `cronograma.html` (2), `gecope_mapa_obras.html` (1) e `portal_transparencia.html` (1) extraídos e parseados via `vm.Script` do Node — sem erro de sintaxe em nenhum. Nenhuma correção das rodadas 4-7 foi revertida ou alterada.

**Requer teste manual no navegador antes de considerar esta revisão 100% validada** (lista consolidada, união do que já vinha pendente das rodadas 9-12 mais o que é específico desta rodada):
1. **Login/cadastro/redefinição de senha** (`landingLoginForm`, `landingRegisterForm`) — fluxo completo, incluindo o botão de mostrar/ocultar senha.
2. **Painel Financeiro**: os 5 filtros multiselect (status/fiscal/contratada/contratante/ano, inclusive "Não informado"), troca de métrica no card "Redução", persistência do filtro após criar/editar/excluir processo em outra aba, busca do drilldown (~250ms).
3. **Aba Aprovados/Suíte**: alerta de pré-diligência aparecendo para processos sem `suite_data_chegada`.
4. **Curva ABC**: upload de planilha, busca com debounce (~200ms), edição rich-text sanitizada.
5. **Painel Admin**: aprovar/recusar usuário e notificação pendente (os 6 pontos `data-*`→`dataset`), linhas de carregamento/erro da tabela (colspan), login do modal admin (`formLogin`).
6. **WhatsApp**: disparo manual, reenvio de mensagem falhada, botão "Ver Detalhes do Erro" (novo `verDetalheErroWhatsApp`), teste de conexão/QR code.
7. **Cronograma**: criar/editar tarefa (observação rich-text sanitizada), motivo de paralisação/alteração de prazo, relatório por analista, calendário, aba Rotinas, histórico (`mh-*`), impressão (`window.print()`).
8. **Mapa de Obras**: busca com debounce (~150ms), filtros dinâmicos (`#filtersHost`), modal de contrato, ranking por distrito/cidade, KPI "Paralisadas" batendo com o gráfico, comissão com suplentes.
9. **Portal da Transparência**: busca por SACC/ID, botões "Ver edital"/"Íntegra do contrato" (URLs `safeUrl()`), proxy `/consulta` e `/edital`.
10. **Confirmar visualmente** que a ausência do botão `#btn-admin-login` (item documentado acima) não é, na verdade, uma regressão visual recente — ou seja, abrir o cabeçalho do sistema e confirmar que não há nenhum ícone de cadeado "quebrado"/sem função visível, e que o controle de visibilidade de módulos por papel (`applyRoleToUI`) está de fato substituindo essa função a contento.
11. Compatibilidade cross-browser/mobile (Bloco 6, pendente desde a auditoria original) — segue fora do alcance de qualquer revisão estática.

## 14. NONA RODADA — Correções pontuais solicitadas pelo usuário (2026-08-14)

Após a revisão em 5 camadas (rodadas 4-8), o usuário revisou a seção 13 e pediu 4 ações específicas.

### 1) Removido — seletor de módulos pós-login (órfão, confirmado pelo Agente 5)
`main.js`: removidos o array `modules`, `buildModuleSelector()`, `openModuleSelector()` e o listener de `#btn-admin-login` (o botão não existe mais em `index.html`; `applyRoleToUI()`/`data-roles` já cobre o controle de visibilidade de abas). `admin.js`: removida `atualizarIconesCadeado()` (só existia para atualizar o ícone desse botão) e suas 5 chamadas; `window.atualizarIconesCadeado` removido do export. `style.css`: removido o bloco `.btn-admin-login`. Confirmado por grep: zero referências residuais em `.js`/`.html`/`.css`.

### 2) Deduplicado — `escapeHTML()`
Estava definida de forma idêntica em `utils.js:209` (exportada explicitamente via `window.escapeHTML`, carregada antes de todos os outros scripts locais) e `main.js:1138` (declaração solta, redundante, que só "vencia" por ser a última carregada). Mantida a versão de `utils.js` — é o módulo de utilitários compartilhados, com exportação explícita para `window`, usado por `dashboard.js` e outros. Removida a redeclaração em `main.js`, com comentário explicando a dependência de ordem de carregamento.

### 3) Corrigido — modal "Recuperar Senha" aparecendo atrás da tela de login
Causa raiz: `.landing-overlay` tem `z-index: 2100` (`style.css`), enquanto o SweetAlert2 usa `z-index: ~1060` por padrão — qualquer `Swal.fire()` aberto a partir da tela de login (login, cadastro, "Esqueci minha senha") renderizava **atrás** do overlay, ficando invisível até o overlay ser escondido (dando a impressão de que a janela só aparecia "depois" da tela de login). Corrigido com `.swal2-container { z-index: 2200 !important; }` em `style.css`, logo após a regra do `.landing-overlay`.

### 4) Corrigido (parcialmente) — e-mail divergente entre `auth.users` e `app_users`
Investigação completa: o repositório já continha `sql/fix_app_users_email_mismatch.sql`, um script de diagnóstico/reparo para contas **já afetadas no passado** por uma versão anterior da Edge Function `approve-user` que fabricava e-mails `matricula@gecope.app` em vez de usar o e-mail real do cadastro — esse ponto específico já havia sido corrigido no código-fonte antes desta sessão.

Porém, a causa raiz **ainda ativa** para novos cadastros foi confirmada em `signUpRequest()` (`main.js`): quando o cadastro encontra um registro "fantasma" (pré-cadastrado por matrícula) e tenta atualizar seu e-mail para o e-mail real via `UPDATE ... WHERE id = existingGhost.id`, essa atualização (e o fallback de `INSERT`) pode falhar silenciosamente por RLS — o código só registrava `console.warn`, e o usuário via a mesma mensagem de sucesso ("Solicitação enviada... aguarde aprovação") independentemente do resultado real. Isso permite que a conta de autenticação (`auth.users`) seja criada com o e-mail correto enquanto `app_users` mantém o e-mail antigo/fabricado — exatamente o sintoma relatado: admin aprova pelo frontend, mas o usuário continua sem conseguir acessar.

**Corrigido:** `signUpRequest()` agora rastreia (`appUsersSynced`) se a gravação em `app_users` de fato ocorreu com o e-mail correto. Se falhar (UPDATE e INSERT de fallback ambos sem sucesso), o usuário recebe um alerta específico e diferente do de sucesso, informando que a conta de login foi criada mas o perfil não foi vinculado, pedindo para contatar o administrador com matrícula e e-mail — em vez do alerta genérico de sucesso que mascarava o problema.

O fluxo de aprovação pelo admin (`aprovarFromNotification`, `admin.js`) já tinha um último fallback de inserção direta (linha ~521-538) usando o e-mail real corretamente priorizado, com erro claro se falhar — não precisou de correção.

**Ação pendente do usuário (não pode ser feita a partir do repositório):** rodar os blocos de DIAGNÓSTICO do `sql/fix_app_users_email_mismatch.sql` no painel do Supabase para identificar contas já afetadas no passado, revisar o resultado e então descomentar/rodar o bloco de REPARO (passo 3) se houver registros divergentes confirmados.

### Validação técnica
`node --check` sem erros em `main.js` e `admin.js`. Nenhum commit foi feito.

## 15. DÉCIMA RODADA — Decisões de segurança do usuário aplicadas (2026-08-14)

### 1) Chave da Evolution API removida do front-end
`config.js`: `EVO_API_URL`/`EVO_API_KEY` zerados (WhatsApp está pausado — sem acesso ao Railway, em reavaliação pelo usuário). `whatsapp.js`: `fetchComTimeout()` (único ponto de saída para a Evolution API, usado pelas 7 chamadas do arquivo) agora lança erro claro e controlado ("Disparo de WhatsApp está pausado...") quando a chave/URL não estão configuradas, em vez de disparar requisições com credenciais em branco. Todos os chamadores já tinham `try/catch` cobrindo isso (confirmado, sem necessidade de alterar cada função individualmente).

### 2) RLS de `app_users` aplicada pelo usuário — ajuste urgente necessário
O usuário rodou `sql/proposta_rls_app_users.sql` em produção. Revisão do código à luz da policy de SELECT ("admin OU só a própria linha") encontrou 3 usos legítimos de leitura ampla que essa policy quebra: login por matrícula (`main.js:136`, chamada **anônima**, pré-autenticação — sempre retorna 0 linhas agora), lista de fiscais (`main.js:815`, roda para todo usuário) e lista de destinatários do WhatsApp (`whatsapp.js:194`, paralisado mas ficaria quebrado se reativado). Além disso, a policy de UPDATE ("só admin") bloqueia o próprio mecanismo `promoteAdmin99030487` na primeira execução, se a conta ainda não estiver marcada como admin na tabela.

Criado `sql/ajuste_rls_app_users_select.sql` (não aplicado — aguardando o usuário rodar): afrouxa SELECT para leitura geral (o risco real era de escrita, não de leitura — esses campos já eram 100% legíveis antes de qualquer RLS existir) e adiciona uma exceção pontual na policy de UPDATE para `99030487@gecope.app`, preservando o mecanismo de autopromoção mantido por decisão do usuário.

**Efeito colateral conhecido, não corrigido:** com UPDATE restrito a admin, o merge de registros "fantasma" (pré-cadastrados por matrícula) durante o cadastro real deixa de conseguir atualizar a linha existente e passa a criar uma segunda linha duplicada — o acesso do usuário não é afetado, mas a lista de usuários fica com uma entrada órfã por matrícula nesse cenário. Não corrigido nesta rodada (fora do pedido original); posso implementar limpeza automática no fluxo de aprovação se solicitado.

### 3) Backdoor `promoteAdmin99030487`: mantido, sem alteração de código (só a exceção de RLS acima, para não quebrá-lo).

### Validação técnica
`node --check` sem erros em `config.js` e `whatsapp.js`. SQL não testado contra banco real (sem acesso). Nenhum commit foi feito.

## 16. DÉCIMA-PRIMEIRA RODADA — Remoção da integração de consulta de contratos (2026-08-14)

Por decisão do usuário ("solução anterior já abandonada"), removida completamente a integração de consulta de contratos via proxy externo:

- **Removido:** diretório `consulta-contratos-proxy/` inteiro (serviço Node + scripts do Windows Service + Dockerfile), arquivo `portal_transparencia.html`.
- **`index.html`:** removido o tile "Portal da Transparência / Licitação" do menu Home.
- **`gecope_mapa_obras.html`:** removido o botão "Portal da Transparência ↗" do modal de detalhe do contrato (`sacBtn`, que linkava para `portal_transparencia.html?sacc=...`) e o CSS órfão `.sac-btn`; a linha "SAC" do modal continua exibindo o valor (`raw.nr_contrato_sic`), só sem o link externo.
- Confirmado por grep: zero referências residuais a `portal_transparencia`/`consulta-contratos-proxy` em `.html`/`.js`/`.css`.

**Não removido (fora do pedido, aguardando decisão):** `functions/approve-user/` — situação semelhante (código presente mas nunca deployado corretamente), mas ainda pode ser usado como fallback opcional no fluxo de aprovação de usuários; só remover se o usuário confirmar que também considera essa peça abandonada.

### Validação técnica
Blocos `<script>` inline de `index.html` e `gecope_mapa_obras.html` parseados via `vm.Script` sem erro de sintaxe. Nenhum commit foi feito.

## 17. DÉCIMA-SEGUNDA RODADA — Remoção do approve-user e correção de gap crítico de RLS (2026-08-14)

### 1) Removido — Edge Function `approve-user` (código parado, nunca implantada)
Diretório `functions/` inteiro removido (`functions/approve-user/index.js`, `functions/README_DEPLOY.md`). `admin.js` (`aprovarFromNotification`): removidos os passos 2 (`sbClient.functions.invoke('approve-user', ...)`) e 3 (fallback HTTP para `/.netlify/functions/approve-user`) — ambos inalcançáveis nesta implantação (sem `supabase/functions/approve-user`, sem `netlify.toml`/`vercel.json` no repo). Mantido apenas o passo de inserção direta via `sbClient`, que já era o único caminho que funcionava de fato. `config.js`: removida a variável `APPROVE_USER_ENDPOINT`, sem mais nenhuma referência no código.

### 2) Achado crítico adicional na RLS — policy de INSERT sem exceção para admin
Ao revisar `admin.js` para a limpeza acima, confirmado que a policy `app_users_insert_self_pending` (de `sql/proposta_rls_app_users.sql`, já aplicada em produção) só permite que o usuário insira a **própria** linha, com `role='pending'` — sem nenhuma exceção para admin. Isso bloqueia dois fluxos que dependem de um admin inserir uma linha para **outro** e-mail: "Novo Usuário" cadastrado manualmente (`admin.js` ~linha 336) e o fallback de inserção direta de `aprovarFromNotification` (linha acima). Adicionada a correção em `sql/ajuste_rls_app_users_select.sql` (bloco 3, ainda não aplicado — junto com a correção de SELECT já registrada na rodada 15).

### 3) Confirmado pelo usuário: `99030487@gecope.app` já é admin hoje
A exceção de UPDATE para essa conta em `sql/ajuste_rls_app_users_select.sql` deixa de ser estritamente necessária (já cobre pela regra normal `app_users_is_admin()`), mas foi mantida como cinto-de-segurança caso o papel seja resetado no futuro.

### Validação técnica
`node --check` sem erros em `admin.js` e `config.js`. Nenhum commit foi feito.

## 18. DÉCIMA-TERCEIRA RODADA — Remoção do backdoor promoteAdmin99030487 (2026-08-14)

Por decisão do usuário (revertendo a decisão anterior de 2026-07-26 de mantê-lo), removida completamente a auto-promoção automática:
- `admin.js`: removida a função `promoteAdmin99030487()`, seu export `window.promoteAdmin99030487` e a chamada `setTimeout(promoteAdmin99030487, 500)` no `DOMContentLoaded` (mantido o resto do listener, que também religa `formLogin`).
- `main.js`: removida a cópia duplicada completa (função + comentário de seção + chamada em `DOMContentLoaded`).
- Confirmado por grep: zero referências residuais a `promoteAdmin99030487` em `.js`/`.html`.

**Usuário confirmou (2026-08-14):** a conta `99030487@gecope.app` já está com `role='admin'` na tabela hoje — a remoção do mecanismo de auto-promoção não deixa essa conta sem acesso.

**Nota sobre a RLS já aplicada:** a exceção para `email = '99030487@gecope.app'` na policy de UPDATE (`sql/ajuste_rls_app_users_select.sql`, já rodada pelo usuário) continua no banco — agora é só uma permissão extra inofensiva (a conta já é admin e passaria por `app_users_is_admin()` de qualquer forma), não corresponde mais a nenhum mecanismo automático no código. Não requer ação.

### Validação técnica
`node --check` sem erros em `admin.js` e `main.js`. Nenhum commit foi feito.

## 19. DÉCIMA-QUARTA RODADA — Consolidação da RLS de app_users (2026-08-14)

Por pedido do usuário ("exclua o que for redundante"), consolidados `sql/proposta_rls_app_users.sql` e `sql/ajuste_rls_app_users_select.sql` (ambos já aplicados em produção, em sequência) num único arquivo `sql/rls_app_users.sql`, removendo os dois originais. O novo arquivo representa o estado final desejado, sem a exceção de UPDATE para `99030487@gecope.app` — redundante desde a remoção do backdoor `promoteAdmin99030487` do código (rodada 18), já que essa conta é admin permanentemente e a regra normal (`app_users_is_admin()`) já cobre o caso.

**Ação pendente do usuário:** rodar `sql/rls_app_users.sql` no Supabase para remover a exceção redundante da policy de UPDATE já aplicada (o script é idempotente — todo `create policy` é precedido de `drop policy if exists`, seguro de rodar mesmo já tendo as policies anteriores aplicadas).

### Validação técnica
Alteração só em arquivos `.sql` (documentação/migração), sem `.js`/`.html` tocados nesta rodada. Nenhum commit foi feito.

## 20. DÉCIMA-QUINTA RODADA — 3 ajustes finais pós-teste manual (2026-08-14)

Usuário confirmou teste manual no navegador realizado ("podemos prosseguir") e pediu 3 ajustes finais:

### 1) Removido — código morto sem gatilho de UI
- `cronograma.html`: popup de detalhe do dia no calendário (`abrirCalPopup()`/`fecharCalPopup()`, markup `#cal-popup`/`.cp-*`, todo o CSS associado incluindo overrides de tema escuro) — nada no calendário chamava `abrirCalPopup()`; as células já mostram os chips de tarefa inline, então o popup nunca abria.
- `whatsapp.js`: `notificarAtualizacaoTabelas()` e `consultarStatusWhatsApp()` — confirmado novamente (reconfirmado por 2 rodadas anteriores) que nenhum botão/handler as chama em todo o repositório.

### 2) Corrigido — duplicação de linha "fantasma" no cadastro
Adicionada em `sql/rls_app_users.sql` uma segunda policy de UPDATE (`app_users_update_claim_ghost`), permissiva e estreita: permite que um usuário autenticado atualize uma linha que hoje tenha e-mail fabricado (`%@gecope.app` ou `%@sop-ghost.internal`), mas só para trocá-lo pelo próprio e-mail autenticado, mantendo `role='pending'`. Isso restaura o comportamento pedido pelo usuário — "sempre atualizar, nunca duplicar" — sem reabrir a permissão ampla de UPDATE que a RLS original corrigiu. `signUpRequest()` (main.js) não precisou de nenhuma alteração: o código já tentava o UPDATE primeiro e só caía no INSERT de fallback quando o UPDATE falhava; com a nova policy, o UPDATE volta a suceder para esse caso específico.

**Ação pendente do usuário:** rodar `sql/rls_app_users.sql` novamente no Supabase (idempotente) para aplicar essa policy adicional.

### 3) Ajustado — seta de tendência do "% Revisão"
`dashboard.js` (drilldown do Painel Financeiro): a seta (▲/▼) ao lado do "% Revisão" era sempre exibida, inclusive quando o valor era exatamente 0,0% (mostrando seta para cima por padrão, sugerindo uma tendência inexistente). Agora a seta só aparece quando `_diffPerc !== 0`; em 0,0% o pill mostra só o percentual, sem ícone. A cor de fundo do pill (positivo/negativo) e a coluna "Diferença" (R$) não foram alteradas — só o ícone de seta do "% Revisão", conforme pedido.

### Validação técnica
`node --check` sem erros em todos os `.js` do sistema. Blocos `<script>` inline de `index.html`, `cronograma.html` e `gecope_mapa_obras.html` parseados via `vm.Script` sem erro. Nenhum commit foi feito — o usuário fará o commit manualmente após concluir os testes.

## 21. DÉCIMA-SEXTA RODADA — Diagnóstico de e-mail divergente concluído (2026-08-14)

Usuário rodou as consultas 1 e 2 de `sql/fix_app_users_email_mismatch.sql` contra o banco de produção:

- **Consulta 2** (candidatos a reparo por UPDATE — perfil existente com e-mail fabricado divergente da notificação): **0 linhas**. Não há nenhum caso de e-mail divergente reparável hoje — o passo 3 (UPDATE) desse script não precisa ser rodado.
- **Consulta 1** (contas de auth.users sem NENHUM perfil em app_users): **6 linhas**, sendo 2 contas com e-mail real aparentando pessoas genuínas sem perfil (`johab.galiza@sop.ce.gov.br`, `grupodenisrildon@gmail.com` — provável efeito do bug de falha silenciosa corrigido na Rodada 14, anterior à correção) e 4 contas com e-mail fabricado (`NNNNNN@gecope.app`) sem perfil, provavelmente anteriores ao fluxo de notificação atual.

Criado `sql/localizar_orfaos_para_cadastro_manual.sql` (só leitura) para localizar nome/matrícula dessas 6 contas via `app_notifications`, apoiando o cadastro manual pelo admin em "Novo Usuário" — ação que só pode ser feita pelo usuário no painel Supabase/Admin, com os dados corretos de cada pessoa.

### Estado da auditoria de segurança/RLS de app_users: CONCLUÍDA
Todas as policies de RLS (SELECT, INSERT, UPDATE — incluindo a de reivindicação de linha fantasma —, DELETE) estão aplicadas e consolidadas em `sql/rls_app_users.sql`. Nenhum caso de e-mail divergente reparável pendente. Restam só os 6 perfis órfãos para cadastro manual, que dependem de identificação humana (não são bug de código).

### Validação técnica
Nenhuma alteração em `.js`/`.html` nesta rodada — só `.sql` (diagnóstico, sem escrita no banco). Nenhum commit foi feito.
